{
  "name": "looktwice-uk",
  "private": true,
  "description": "Dev tooling for looktwice.uk — test runner only, not shipped to the site",
  "scripts": {
    "dev": "browser-sync start --server . --port 3000 --no-notify --files \"index.html,css/*.css,js/*.js,images/*\"",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "browser-sync": "^3.0.4"
  }
}
