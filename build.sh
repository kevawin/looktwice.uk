#!/usr/bin/env bash
# No-op build script.
# This site is static HTML and does not need a build step.
# This file exists only because the Cloudflare Pages project is
# configured to run `bash build.sh`. Once that build command is
# cleared in the Cloudflare dashboard (Settings → Builds & deployments
# → Build configurations → Build command: leave empty), this file
# can be deleted.
exit 0
