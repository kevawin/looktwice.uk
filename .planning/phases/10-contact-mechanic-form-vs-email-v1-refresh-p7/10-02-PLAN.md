---
phase: 10-contact-mechanic-form-vs-email-v1-refresh-p7
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - CLAUDE.md
  - .planning/STATE.md
  - tests/contact-form.spec.js
autonomous: true
requirements: []   # No formal REQ IDs — see must_haves (derived from D-01 reversal + Playwright lean)
must_haves:
  truths:
    - "CLAUDE.md states a V1 contact rule: Formspree contact form, no visible mailto — the mailto lock is reversed (D-01) with rationale recorded"
    - "STATE.md mailto lock is reversed and the reversal + rationale (Jamie review + Kris decision 2026-06-01) is recorded"
    - "A Playwright spec covers the form: submit happy path, required-field validation, honeypot, and success + error states"
    - "npm test passes, hitting a mocked Formspree route so no real submission lands in Kris's inbox"
  artifacts:
    - path: "CLAUDE.md"
      provides: "V1 contact rule citing the Formspree endpoint and the D-01/D-02 reversal"
      contains: "Formspree"
    - path: ".planning/STATE.md"
      provides: "Decision-reversal entry replacing the 'mailto in V1, no form' line"
      contains: "reversed"
    - path: "tests/contact-form.spec.js"
      provides: "Playwright E2E coverage of the contact form with a mocked Formspree endpoint"
      contains: "page.route"
  key_links:
    - from: "tests/contact-form.spec.js"
      to: "Formspree endpoint"
      via: "page.route intercept returns a stubbed 200/4xx — no real POST"
      pattern: "page\\.route"
---

<objective>
Reconcile the rule reversal in documentation and add automated end-to-end coverage for the new form. D-01/D-02 reverse the locked "mailto in V1, no form" rule — CLAUDE.md and STATE.md must record the change with rationale. Add a Playwright spec (the repo already has Playwright set up) that exercises the form without sending real submissions to Kris's inbox.

Purpose: Keep planning artifacts honest about the reversal, and gate the form behind automated tests.
Output: Updated CLAUDE.md + STATE.md, plus a passing `tests/contact-form.spec.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-contact-mechanic-form-vs-email-v1-refresh-p7/10-CONTEXT.md
@.planning/phases/10-contact-mechanic-form-vs-email-v1-refresh-p7/10-PATTERNS.md
@.planning/phases/10-contact-mechanic-form-vs-email-v1-refresh-p7/10-01-PLAN.md

<interfaces>
<!-- Confirmed from codebase reads 2026-06-01. -->

CLAUDE.md state: the project CLAUDE.md Constraints block has NO existing mailto/contact bullet (grep confirmed: no "mailto" and no "email link" string in CLAUDE.md). The mailto lock lives only in STATE.md. So in CLAUDE.md you ADD a new contact rule — there is no old rule string to delete there.

Playwright IS already configured (dev-only, node_modules gitignored, not shipped):
- `playwright.config.js`: testDir `./tests`, baseURL `http://localhost:7777`, webServer `python3 -m http.server 7777` (reuseExistingServer), three viewport projects (mobile-375, tablet-768, desktop-1440).
- `package.json`: `"test": "playwright test"`, devDependency `@playwright/test ^1.48.0`.
- Existing spec `tests/nav-floating-bar.spec.js` uses `const { test, expect } = require('@playwright/test');`, helpers `scrollTo`/`scrollIntoView`/`computedStyle`. Match this CommonJS style.

Form selectors (from Plan 01): `#contact-form`, `#contact-status`, `input[name="name"]`, `input[name="email"]`, `textarea[name="message"]`, `input[name="_gotcha"]`, `button.contact__submit`. The form `action` is `https://formspree.io/f/xbdbnrkr`.

STATE.md current line to replace (Decisions section, ~line 74): `- Email link (`mailto:`) in V1, no form`. Confirmed present (grep count 1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the mailto→form reversal in CLAUDE.md and STATE.md</name>
  <read_first>
    - CLAUDE.md (Project › Constraints list — confirm there is no existing contact/mailto bullet)
    - .planning/STATE.md lines 68-128 (Decisions section; `Email link (mailto:) in V1, no form` is ~line 74)
    - .planning/phases/10-contact-mechanic-form-vs-email-v1-refresh-p7/10-CONTEXT.md D-01 (reversal rationale)
    - .planning/phases/10-contact-mechanic-form-vs-email-v1-refresh-p7/10-PATTERNS.md "CLAUDE.md" + "STATE.md" sections (lines 359-386)
  </read_first>
  <action>
    1. CLAUDE.md: ADD a contact rule to the Project › Constraints block (no pre-existing mailto bullet exists there to remove). Text (no em-dashes; hyphens in number-word compounds OK; use commas not em-dashes for asides):
       `- **Contact**: V1 contact = Formspree contact form at https://formspree.io/f/xbdbnrkr. No visible mailto links and no visible email address. The form is the single contact route (D-01/D-02 reversal, Jamie review plus Kris decision 2026-06-01). Plain HTML plus vanilla JS fetch, no SDK.`
       Keep all design bans intact.
    2. .planning/STATE.md: Replace the line `- Email link (`mailto:`) in V1, no form` (~line 74) with (NO em-dash — use commas/parentheses):
       `- V1 contact = Formspree form (reversed 2026-06-01 per D-01/D-02 in Phase 10; mailto plus visible email removed entirely; rationale: Jamie review plus Kris decision)`
       Do not delete surrounding decision lines. Single-line replacement recording the reversal.
  </action>
  <verify>
    <automated>grep -qi 'Formspree' CLAUDE.md && grep -q 'formspree.io/f/xbdbnrkr' CLAUDE.md && grep -qi 'reversed' .planning/STATE.md && test "$(grep -c 'mailto:`) in V1, no form' .planning/STATE.md)" = "0" && ! grep -qP '\xe2\x80\x94' CLAUDE.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source: CLAUDE.md contains a contact bullet naming Formspree, the endpoint `formspree.io/f/xbdbnrkr`, and citing the D-01/D-02 reversal with date 2026-06-01.
    - Source: STATE.md no longer has `Email link (mailto:) in V1, no form`; the replacement line contains "reversed" and the rationale.
    - Source: no em-dash (U+2014) characters introduced in either edit (`grep -P '\xe2\x80\x94'` finds none in the new lines).
    - Behavior: the docs collectively state the form-only decision; neither still asserts mailto-only.
  </acceptance_criteria>
  <done>Both docs reflect the form-only decision and explain the reversal; the old mailto lock is gone from STATE.md and a Formspree contact rule is present in CLAUDE.md.</done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright E2E spec for the contact form (mocked Formspree)</name>
  <read_first>
    - tests/nav-floating-bar.spec.js lines 1-60 (CommonJS style, helpers, viewport projects)
    - playwright.config.js (baseURL, webServer, projects)
    - index.html contact form markup from Plan 01 (selectors + action URL)
    - js/main.js initContactForm (success/error behavior: form.hidden on success, status.textContent messages, honeypot guard, focus-first-invalid)
  </read_first>
  <action>
    Create tests/contact-form.spec.js using `require('@playwright/test')` (match the existing spec style). Cover the four scenarios from the roadmap Playwright lean. Use `page.route('**/formspree.io/**', ...)` to intercept the POST so NO real submission reaches Kris's inbox.

    Tests:
    1. Happy path: navigate to `/`, fill name + email + message, stub the Formspree route to `route.fulfill({ status: 200, contentType: 'application/json', body: '{"ok":true}' })`, click Send. Assert the form becomes hidden (`#contact-form` has the `hidden` attribute) and `#contact-status` shows non-empty success text. Assert the page did not navigate (URL still ends `/`).
    2. Required-field validation: leave a required field empty, click Send. Track whether the Formspree route fired (increment a counter in the route handler); assert it did NOT fire, `#contact-status` shows the validation message, and focus is on the first invalid field.
    3. Honeypot: set `input[name="_gotcha"]` to a value (via `page.fill` after un-hiding, or `page.evaluate` to set `.value`), fill the real fields, click Send. Assert NO Formspree request fired (bot path returns silently).
    4. Error state: stub the route to `route.fulfill({ status: 500, contentType: 'application/json', body: '{}' })`, submit valid fields, assert `#contact-status` shows an error/retry message AND that the status text matches NEITHER `/@/` NOR `/mailto/i` (enforces D-02 — no email in error copy).
    Add one assertion that the page has zero mailto links: `expect(await page.locator('a[href^="mailto:"]').count()).toBe(0)`.
    Scope the suite to a single project where viewport variation is irrelevant to form logic (viewport coverage already lives in the nav spec) — e.g. plain `test()` blocks, no per-viewport branching.
  </action>
  <verify>
    <automated>npx playwright test contact-form 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File tests/contact-form.spec.js exists and uses `page.route` to mock the Formspree endpoint (no real POST).
    - `npx playwright test contact-form` passes: happy-path, validation, honeypot, error-state tests all green.
    - Error-state test asserts the status message contains no `@` and no `mailto` (D-02 guard).
    - A test asserts `a[href^="mailto:"]` count is 0 on the page.
    - Honeypot + validation tests assert NO Formspree request fired.
  </acceptance_criteria>
  <done>Playwright spec covers happy path, validation, honeypot, and error states against a mocked endpoint; `npm test` is green; no real submissions sent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → Formspree | Tests must NOT hit the real endpoint; all submission traffic is intercepted and stubbed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02b | Information disclosure / inbox spam | Playwright submit tests | mitigate | All tests intercept `**/formspree.io/**` via `page.route` and fulfill locally — no real POST reaches Kris's inbox. |
| T-10-03b | Information disclosure | error-state assertion | mitigate | Error-state test asserts no email address (`/@/`, `/mailto/i`) in the status text, locking in the D-02 form-only guarantee against regression. |
| T-10-SC | Tampering (supply chain) | npm devDependencies | accept | Playwright (`@playwright/test ^1.48.0`) is already installed, dev-only, gitignored, not shipped. No NEW package install in this phase. RESEARCH package-legitimacy gate not triggered (no install task). |
</threat_model>

<verification>
- `npm test` (or `npx playwright test`) passes — both the existing nav spec and the new contact spec.
- STATE.md and CLAUDE.md both describe the form-only decision; STATE.md no longer asserts mailto-only.
- No em-dash introduced in either doc edit.
</verification>

<success_criteria>
- CLAUDE.md and STATE.md record the mailto→form reversal with rationale and date.
- A passing Playwright spec covers submit, validation, honeypot, and error states against a mocked Formspree endpoint.
- No test sends a real submission to Kris's inbox.
</success_criteria>

<output>
Create `.planning/phases/10-contact-mechanic-form-vs-email-v1-refresh-p7/10-02-SUMMARY.md` when done.
</output>
