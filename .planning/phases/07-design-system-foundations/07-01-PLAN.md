---
phase: 07-design-system-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/tokens.css
  - css/components.css
  - index.html
  - CLAUDE.md
autonomous: true
requirements: [V1-REFRESH-P1]
user_setup: []

must_haves:
  truths:
    - "Every sub-label on the page renders at 14px or larger (no sub-16px prose anywhere)"
    - "The page shows exactly two CTA buttons: hero and contact — the work and services mid-page CTAs are gone"
    - "Both remaining CTA buttons read 'Free 30-min chat'"
    - "The hero pre-button label adds a reassurance value line, not a copy of the button text"
    - "No accent-variant button styles remain in the stylesheet and no HTML references them"
    - "The brand gradient still appears in exactly one place (the floating sticky tab) — no button paints a gradient"
    - "CLAUDE.md still bans em-dashes but explicitly allows hyphens in number-word content"
  artifacts:
    - path: "css/tokens.css"
      provides: "--text-label token at 0.875rem (14px floor)"
      contains: "--text-label:    0.875rem"
    - path: "css/components.css"
      provides: "Button system with only .btn, .btn--on-pink, .btn--on-teal"
      contains: ".btn--on-teal"
    - path: "index.html"
      provides: "Two CTAs reading 'Free 30-min chat', reworded hero label, no mid-page CTAs"
      contains: ">Free 30-min chat<"
    - path: "CLAUDE.md"
      provides: "Relaxed em-dash ban allowing number-word hyphens"
      contains: "em-dash"
  key_links:
    - from: "css/components.css .btn"
      to: "var(--text-label)"
      via: "font-size on button base"
      pattern: "font-size: var\\(--text-label\\)"
    - from: "index.html hero CTA"
      to: "#contact"
      via: "anchor href"
      pattern: "btn--on-pink.*#contact"
---

<objective>
Settle three global primitives for the V1 Refresh: raise the label font token to a 14px floor, collapse the button system to two surviving variants by removing the redundant mid-page CTAs and dropping unused accent CSS, unify the remaining CTA copy to one string, reword the hero pre-button label to a value line, and relax the CLAUDE.md em-dash ban for number-word hyphens.

Purpose: Every later refresh phase needs consistent type, one button idiom, and one CTA string. This phase is a focused refactor — a token bump, button-variant removal, and copy swap. No new sections, no layout redesign.

Output: Edited css/tokens.css, css/components.css, index.html, CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-design-system-foundations/07-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Exact current source the executor edits. Use these directly — no codebase exploration needed. -->

css/tokens.css:50 (current):
  --text-label:    0.8rem;
  (used 11× in css/components.css via var(--text-label); zero uses in other css files. .btn base sets font-size: var(--text-label) at components.css:177.)

css/components.css — surviving button variants to KEEP:
  .btn (base, line 171), .btn--on-pink (line 191), .btn--on-teal (line 209)
css/components.css — variants to DELETE (lines 227-273):
  .btn--accent-pink (228), .btn--accent-indigo (241), .btn--accent-amber (254),
  and the shared accent focus-ring block (268-273: .btn--accent-pink:focus-visible, .btn--accent-indigo:focus-visible, .btn--accent-amber:focus-visible)
css/components.css:623-626 — .work__cta rule (margin/align helper for the removed work CTA anchor; becomes dead after the anchor is removed)

index.html exact current strings:
  127: <p class="hero__cta-label">Schedule a free 30-minute diagnosis.</p>
  129: <a class="btn btn--on-pink" href="#contact">Let's talk</a>
  221: <a class="btn btn--accent-indigo work__cta" href="#contact">Get in touch</a>
  264: <a class="btn btn--accent-pink" href="#contact">Let's talk</a>
  279: <a class="btn btn--on-teal" href="mailto:hello@looktwice.uk">Let's talk</a>
  315: <span class="sticky-tab__label">Let's talk &rarr;</span>  ← DO NOT EDIT (sticky tab is refresh Phase 2 / GSD 08 scope; D-02 scopes relabel to lines 129 and 279 only)

No data-reveal / reveal-index attributes exist on any element (grep confirms zero) — removing the CTAs orphans no reveal wiring.

CLAUDE.md:17 (the design-bans line, current):
  - **Design bans (hard stops)**: no card shadows, no gradient text, no glassmorphism, no mid-tone greys, no decorative card grids, no font-weight 500, no em-dashes in copy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise the label token to the 14px floor</name>
  <files>css/tokens.css</files>
  <read_first>css/tokens.css (lines 40-58), css/components.css (lines 171-188, the .btn base that consumes --text-label)</read_first>
  <action>Per D-08, change `--text-label` at css/tokens.css:50 from `0.8rem` to `0.875rem` (12.8px → 14px). Keep the same alignment/whitespace style as the surrounding token lines. This is the only sub-16px font in the codebase; the token feeds the `.btn` base font-size (components.css:177) plus 10 other component uses, so this single edit lifts every sub-label to the 14px floor. Do not touch any other token. Prose already floors at 16px via `--text-body` clamp min of `1rem` — leave it.</action>
  <acceptance_criteria>
    - `grep -c -- '--text-label:    0.875rem' css/tokens.css` returns 1
    - `grep -c -- '--text-label:    0.8rem' css/tokens.css` returns 0
    - `grep -rn '0\.8rem\|0\.75rem\|12px\|13px\|0\.8125rem' css/` returns no font-size declaration below 14px (audit confirms no other sub-16px literal font)
  </acceptance_criteria>
  <verify>
    <automated>grep -c -- '--text-label:    0.875rem' css/tokens.css</automated>
  </verify>
  <done>--text-label is 0.875rem; no other sub-14px literal font-size exists in css/.</done>
</task>

<task type="auto">
  <name>Task 2: Remove mid-page CTAs and drop unused accent CSS</name>
  <files>index.html, css/components.css</files>
  <read_first>index.html (lines 210-266), css/components.css (lines 227-273 accent variants, lines 623-626 .work__cta)</read_first>
  <action>Per D-04, remove two anchor elements from index.html: the work-section CTA at line 221 (`<a class="btn btn--accent-indigo work__cta" href="#contact">Get in touch</a>`) and the services-section CTA at line 264 (`<a class="btn btn--accent-pink" href="#contact">Let's talk</a>`). Delete the full anchor lines. No wrapper container holds only these anchors (work CTA sits directly in `.work__inner` after `.work__segue`; services CTA sits directly in `.services__inner` after `.services__segue`), so no empty wrapper is orphaned — do not remove the surrounding section divs. No data-reveal attributes exist on these anchors, so no reveal-index cleanup is needed. Per D-05, delete from css/components.css the three unused variant rule blocks `.btn--accent-pink` (line 228), `.btn--accent-indigo` (241), `.btn--accent-amber` (254) including their `:hover, :focus-visible` companions, and the shared accent focus-ring block at 268-273. Also remove the now-dead `.work__cta` rule (components.css:623-626) since its only consumer was the removed work anchor. Keep `.btn`, `.btn--on-pink`, `.btn--on-teal` and their focus-ring overrides untouched. Keep the comment header for the on-colour variants; you may keep or tidy the "On-Linen variants" comment at line 227 (it now describes deleted rules — remove that comment line for cleanliness).</action>
  <acceptance_criteria>
    - `grep -c 'btn--accent' index.html` returns 0
    - `grep -c 'btn--accent' css/components.css` returns 0
    - `grep -c 'work__cta' index.html` returns 0
    - `grep -c 'work__cta' css/components.css` returns 0
    - `grep -c 'btn--on-pink' css/components.css` returns 2 (variant + focus-visible block still present)
    - `grep -c 'btn--on-teal' css/components.css` returns 2
    - `grep -c '>Get in touch<' index.html` returns 0
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'btn--accent' index.html css/components.css | grep -v ':0' | wc -l | tr -d ' ')" = "0" && grep -c 'btn--on-teal' css/components.css</automated>
  </verify>
  <done>No accent-variant classes remain in HTML or CSS; .work__cta rule gone; surviving on-colour variants intact.</done>
</task>

<task type="auto">
  <name>Task 3: Unify CTA copy and reword the hero label</name>
  <files>index.html</files>
  <read_first>index.html (lines 118-131 hero, lines 268-282 contact)</read_first>
  <action>Per D-01/D-02, change the inner text of the two REMAINING CTAs to the single string `Free 30-min chat`: the hero CTA at index.html:129 (currently "Let's talk", class `btn btn--on-pink`, href `#contact`) and the contact mailto CTA at index.html:279 (currently "Let's talk", class `btn btn--on-teal`, href `mailto:hello@looktwice.uk`). Change only the link text — keep classes and hrefs. Do NOT touch the sticky-tab label at line 315 (refresh Phase 2 / GSD 08 scope). The "30-min" hyphen is allowed (it is a hyphen, not an em-dash, and Task 4 relaxes the ban for number-word hyphens). Per D-07, reword the hero pre-button label at index.html:127 (currently "Schedule a free 30-minute diagnosis.") to the provisional value line `No sale, no follow-up unless you want one.` — this must add a reassurance frame, not echo the button's free/30-min message. Add an inline HTML comment immediately before or after that `<p class="hero__cta-label">` noting the wording is provisional and Kris-confirmed in refresh P4, e.g. an HTML comment containing the word `provisional`. Introduce no em-dashes anywhere; "follow-up" and "30-min" use plain hyphens.</action>
  <acceptance_criteria>
    - `grep -c '>Free 30-min chat<' index.html` returns 2
    - `grep -c '>Let.s talk<' index.html` returns 1 (only the line-315 sticky-tab label remains, and it reads "Let's talk &rarr;" not a plain ">Let's talk<" close — so this should actually be 0; confirm only the sticky tab keeps "Let's talk" text)
    - `grep -c 'No sale, no follow-up unless you want one.' index.html` returns 1
    - `grep -c 'Schedule a free 30-minute diagnosis' index.html` returns 0
    - `grep -ci 'provisional' index.html` returns at least 1 (the Kris-confirm note comment)
    - `grep -c '—' index.html` returns 0 (no em-dash introduced)
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '>Free 30-min chat<' index.html)" = "2" && test "$(grep -c 'Schedule a free 30-minute diagnosis' index.html)" = "0" && grep -c '—' index.html</automated>
  </verify>
  <done>Both surviving CTAs read "Free 30-min chat"; hero label is the reassurance value line with a provisional-note comment; no em-dash introduced; sticky tab untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Relax the CLAUDE.md em-dash ban for number-word hyphens</name>
  <files>CLAUDE.md</files>
  <read_first>CLAUDE.md (line 17, the design-bans hard-stops line)</read_first>
  <action>Per D-09, edit the design-bans line at CLAUDE.md:17. Keep the em-dash ban but add an explicit allowance for hyphens in number-word content. Change the trailing clause `no em-dashes in copy.` to wording that keeps the em-dash ban and explicitly permits number-word hyphens — e.g. `no em-dashes in copy (hyphens in number-word compounds such as "30-min" are allowed).` Do not alter any other ban in the list (card shadows, gradient text, glassmorphism, mid-tone greys, decorative card grids, font-weight 500 all stay). This is a docs-only edit; introduce no em-dash in the edited line.</action>
  <acceptance_criteria>
    - `grep -c 'no em-dashes in copy' CLAUDE.md` returns 1 (ban still present)
    - `grep -ci 'hyphen' CLAUDE.md` returns at least 1 (allowance added)
    - `grep -c 'font-weight 500' CLAUDE.md` returns at least 1 (other bans untouched)
    - `grep -c '—' CLAUDE.md` returns 0 on line 17 context (no em-dash introduced by the edit)
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'no em-dashes in copy' CLAUDE.md && grep -ci 'hyphen' CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md keeps the em-dash ban and explicitly allows number-word hyphens; all other design bans intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Raised the label font token to 14px (uppercase button text grows ~1.2px), removed the work and services mid-page CTAs, unified the two surviving CTAs to "Free 30-min chat", and reworded the hero pre-button label to a reassurance line. Pushed to the Cloudflare Pages branch preview.</what-built>
  <how-to-verify>
    Per Claude's Discretion in D-08, this confirms the uppercase button-text growth does not overflow the hero or contact buttons at any breakpoint, and the layout reads correctly after the mid-page CTAs are gone.
    1. Open the branch preview URL on a phone and a desktop browser.
    2. Hero: the button reads "Free 30-min chat" and sits on one line inside the pill at all widths (no wrap, no clipping). The line just above it reads "No sale, no follow-up unless you want one." (a reassurance line, not a repeat of the button).
    3. Contact section: the button also reads "Free 30-min chat" on one line.
    4. Work section: there is no "Get in touch" button after the "Want to see an example..." line — confirm the section still ends cleanly with no leftover gap or stray spacing.
    5. Services section: there is no "Let's talk" button after "the first 30 minutes are on me." — confirm clean ending.
    6. Narrow the browser to ~375px and confirm both buttons still fit without the text overflowing the pill edge.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks off (e.g. button text wrapping, a leftover gap where a CTA was removed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Static HTML/CSS/docs edits only. No user input, no new code execution path, no network or storage boundary introduced. No package installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | CSS variant removal | accept | Pure deletion of unused selectors; surviving `.btn`, `.btn--on-pink`, `.btn--on-teal` retain their focus-ring overrides, so no keyboard-accessibility regression. Verified by grep that survivors stay intact. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (plain HTML/CSS/JS, no build step) — supply-chain surface is unchanged. |
</threat_model>

<verification>
- `grep -c -- '--text-label:    0.875rem' css/tokens.css` returns 1
- `grep -c 'btn--accent' index.html css/components.css` returns 0 across both files
- `grep -c 'work__cta' index.html css/components.css` returns 0 across both files
- `grep -c '>Free 30-min chat<' index.html` returns 2
- `grep -c 'Schedule a free 30-minute diagnosis' index.html` returns 0
- `grep -c '—' index.html css/tokens.css css/components.css CLAUDE.md` returns 0 (no em-dash introduced)
- `grep -c 'no em-dashes in copy' CLAUDE.md` returns 1 and `grep -ci 'hyphen' CLAUDE.md` returns >= 1
- Gradient discipline preserved: `grep -c 'gradient' css/components.css` shows no new gradient on any `.btn` rule (button work paints no gradient)
- Human checkpoint: uppercase button text fits the pill at 375/768/desktop; no overflow after the 14px bump
</verification>

<success_criteria>
1. All sub-labels floor at 14px via the `--text-label` bump; no other sub-16px literal font in css/.
2. The two mid-page CTAs are removed from index.html; the three accent variants and the dead `.work__cta` rule are gone from components.css; `.btn`, `.btn--on-pink`, `.btn--on-teal` survive.
3. The two remaining CTAs read "Free 30-min chat" — one string sitewide (sticky-tab label left for GSD 08).
4. The brand gradient still appears only on the floating sticky tab; no button paints a gradient.
5. CLAUDE.md keeps the em-dash ban and explicitly allows number-word hyphens.
6. The hero pre-button label is a reassurance value line (provisional, Kris-confirm in P4), not a duplicate of the button.
</success_criteria>

<output>
Create `.planning/phases/07-design-system-foundations/07-01-SUMMARY.md` when done.
</output>
