---
phase: 08-nav-floating-bar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - css/components.css
  - css/layout.css
  - css/animations.css
  - js/main.js
autonomous: true
requirements:
  - "REFRESH-P2-HEADER-SCROLLAWAY"
  - "REFRESH-P2-DEBURGER"
  - "REFRESH-P2-DROP-CONTACT"
  - "REFRESH-P2-NAV-ORDER"
  - "REFRESH-P2-GUTTER-ALIGN"
user_setup: []

must_haves:
  truths:
    - "The header scrolls away with the page (no longer fixed/sticky)"
    - "No scroll-driven background/colour change happens on the header"
    - "There is no hamburger button and no full-screen overlay anywhere in the page"
    - "The header menu shows Approach then Work, with no Contact item"
    - "Header links stay visible at 375px (no hamburger collapse)"
    - "Header left/right padding matches the section gutter (logo flush left, links right)"
  artifacts:
    - path: "index.html"
      provides: "Header with logo + Approach/Work links only; no hamburger, no overlay markup"
      contains: "href=\"#approach\""
    - path: "css/components.css"
      provides: "Non-fixed .nav block; hamburger + overlay CSS removed"
    - path: "css/layout.css"
      provides: ".nav padding on the shared gutter; mobile nav-links display:none rule removed"
    - path: "css/animations.css"
      provides: "nav + nav-overlay transition blocks removed"
    - path: "js/main.js"
      provides: "scroll-state toggle + hamburger/overlay JS removed"
  key_links:
    - from: "header nav links"
      to: "#approach / #work section anchors"
      via: "in-page anchor href"
      pattern: "href=\"#approach\""
---

<objective>
Refactor the top header so it stops being persistent and scrolls away with the page, and strip out every piece of the old mobile-nav machinery (3-line hamburger, full-screen overlay, scroll-driven colour fade). Drop "Contact" from the menu, reorder the remaining items to page DOM order (Approach then Work, per D-01), keep the links visible on mobile (no hamburger, per D-06), and align the header padding to the shared section gutter (D-05).

This is the first half of Phase 08. It deletes the code the floating bar (Plan 02) replaces, so the two never both own a menu mechanism. Deletion is the bulk of the work here — see the PATTERNS.md orphan-deletion table.

Purpose: Header becomes a normal in-flow element on the Hot Pink hero that scrolls off; once it is gone, Plan 02's floating bar takes over. No double-menu (success criteria 1 + 2).
Output: Edited index.html, components.css, layout.css, animations.css, js/main.js with all header/overlay/hamburger/scroll-fade code removed and the menu reordered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-nav-floating-bar/08-CONTEXT.md
@.planning/phases/08-nav-floating-bar/08-PATTERNS.md
@./CLAUDE.md

<interfaces>
<!-- Verified live line numbers (2026-06-01). Trust these over CONTEXT/PATTERNS line refs where they differ. -->

index.html:
- :80-98   <nav class="nav"> — wordmark + hamburger button (84-92) + <ul class="nav-links"> (93-97) with WORK / APPROACH / CONTACT in that order
- :100-109 <div id="nav-overlay"> — full-screen overlay (close button + mobile nav links)
- :188     <section id="approach"> (the "Dig. Reveal. Sharpen." interrupt) — DOM order: approach BEFORE work
- :200     <section id="work">
- :28-32   CSS <link> tags at ?v=5 (do NOT bump here — cache-bust bump lives in Plan 02 to land once)
- :317     <script src="/js/main.js?v=5"> (do NOT bump here)

css/components.css:
- :8-16    .nav { position:fixed; top/left/right:0; z-index:100; background:transparent; color:white }  → make non-fixed
- :18-21   .nav.scrolled { background:linen; color:midnight }  → REMOVE (orphan once JS toggle gone)
- :23-83   .nav-wordmark / .nav-logo / .nav-link / .nav-link::after / focus-ring overrides  → KEEP (still valid)
- :80-83   .nav.scrolled .nav-link:focus-visible / .nav.scrolled .nav-wordmark:focus-visible  → REMOVE (.scrolled never set)
- :87-112  .nav-hamburger / .nav-hamburger-line / @media(max-width:1024px){.nav-hamburger}  → REMOVE whole block
- :116-162 .nav-overlay / .open / -close / nav / -link  → REMOVE whole block

css/layout.css:
- :13-19   .nav { display:flex; align-items:center; justify-content:space-between; gap:--space-md; padding:--space-sm calc(--space-lg * 0.75) }  → KEEP flex; confirm padding on shared gutter
- :21-33   .nav-links / li  → KEEP
- :35-37   @media(max-width:1024px){ .nav-links{display:none} }  → REMOVE (D-06 keeps links visible)

css/animations.css:
- :4-7     .nav { transition: var(--transition-nav) }  → REMOVE (no colour fade once non-fixed)
- :9-20    .nav-overlay transition + its reduced-motion guard  → REMOVE whole block
- :22-43   word-roller block  → KEEP (do not touch)

js/main.js:
- :12      if ('scrollRestoration' in history) ...  → REMOVE (no other consumer)
- :14-19   const nav + toggleScrolled + scroll listener  → REMOVE
- :25-59   hamburger / overlay / closeBtn vars + openOverlay/closeOverlay + listeners  → REMOVE whole block
- :68-100  initStickyTab IIFE  → KEEP UNTOUCHED (Plan 02 extends it)
- :106-164 initWordRoller IIFE  → KEEP UNTOUCHED

Shared gutter expression (do NOT hardcode 48px): calc(var(--space-lg) * 0.75)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make header non-fixed and delete the scroll-fade CSS + JS</name>
  <files>css/components.css, css/animations.css, js/main.js</files>
  <read_first>
    - js/main.js (lines 1-100 — scroll-state block + initStickyTab to confirm boundary)
    - css/components.css (lines 8-83 — .nav block + focus-ring overrides)
    - css/animations.css (whole file, 42 lines)
    - .planning/phases/08-nav-floating-bar/08-CONTEXT.md (D-03)
    - .planning/phases/08-nav-floating-bar/08-PATTERNS.md (Orphaned Selectors table)
  </read_first>
  <action>
    Implements D-03. In css/components.css, edit the .nav rule (:8-16) so the header is a normal in-flow element: remove position:fixed, top, left, right, z-index, the transparent background, and the white color declaration so .nav no longer floats over content. Keep whatever .nav declarations are needed for it to read correctly sitting on the Hot Pink hero (it inherits the hero surface). Delete the .nav.scrolled rule (:18-21) and the .nav.scrolled .nav-link:focus-visible / .nav.scrolled .nav-wordmark:focus-visible rules (:80-83) — these are orphaned once the JS toggle is gone. Keep .nav-wordmark, .nav-logo, .nav-link, .nav-link::after, and the non-.scrolled focus-ring overrides (:74-78).

    In css/animations.css, delete the .nav { transition: var(--transition-nav); } rule (:4-7) — no colour fade is needed once the header is non-fixed. Leave the word-roller block intact.

    In js/main.js, delete the scrollRestoration line (:12) and the entire nav scroll-state block (:14-19: the const nav, toggleScrolled, the initial call, and the scroll listener). Update the file's top comment block (:1-6) to drop the "Nav scroll-state toggle" bullet. Do NOT touch initStickyTab (:68-100) or initWordRoller — Plan 02 extends initStickyTab.

    Do NOT bump the ?v=5 cache-bust here — Plan 02 owns the single bump so it lands once across all touched files.
  </action>
  <verify>
    <automated>grep -c 'position: fixed' css/components.css | grep -qv '^0$' && echo "WARN check .nav specifically"; ! grep -q 'toggleScrolled' js/main.js && ! grep -q 'scrollRestoration' js/main.js && ! grep -q '\.nav\.scrolled' css/components.css && echo PASS-JS-CSS</automated>
  </verify>
  <acceptance_criteria>
    - Source: `js/main.js` does NOT contain `toggleScrolled`
    - Source: `js/main.js` does NOT contain `scrollRestoration`
    - Source: `js/main.js` does NOT contain `nav.classList`
    - Source: `css/components.css` does NOT contain `.nav.scrolled`
    - Source: `css/components.css` `.nav` rule does NOT contain `position: fixed`
    - Source: `css/animations.css` does NOT contain `transition: var(--transition-nav)`
    - Source: `js/main.js` STILL contains `initStickyTab` and `initWordRoller` (untouched)
    - Source: `css/animations.css` STILL contains `.word-roller`
  </acceptance_criteria>
  <done>Header is a non-fixed in-flow element; no scroll-driven colour change exists in CSS or JS; initStickyTab and word-roller are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Delete hamburger + overlay (markup, CSS, JS)</name>
  <files>index.html, css/components.css, css/animations.css, js/main.js</files>
  <read_first>
    - index.html (lines 80-110 — nav + overlay markup)
    - css/components.css (lines 85-162 — hamburger + overlay blocks)
    - css/animations.css (lines 9-20 — overlay transition + reduced-motion guard)
    - js/main.js (lines 21-59 — hamburger overlay block)
    - .planning/phases/08-nav-floating-bar/08-CONTEXT.md (D-04)
    - .planning/phases/08-nav-floating-bar/08-PATTERNS.md (Orphaned Selectors table)
  </read_first>
  <action>
    Implements D-04. In index.html, delete the entire <button class="nav-hamburger"> block (:84-92, all three spans included) from inside the .nav, and delete the entire <div id="nav-overlay"> block (:100-109, close button + mobile nav + its links). The .nav after this edit contains only the <a class="nav-wordmark"> and the <ul class="nav-links">.

    In css/components.css, delete the whole .nav-hamburger block: .nav-hamburger, .nav-hamburger-line, and the @media (max-width:1024px){ .nav-hamburger{display:inline-flex} } rule (:87-112). Delete the whole overlay block: .nav-overlay, .nav-overlay.open, .nav-overlay-close, .nav-overlay nav, .nav-overlay-link (:116-162).

    In css/animations.css, delete the .nav-overlay { transition: ... } rule and the @media (prefers-reduced-motion: reduce){ .nav-overlay{transition:none} } guard (:9-20).

    In js/main.js, delete the entire hamburger overlay block (:25-59): the hamburger/overlay/closeBtn const declarations, openOverlay, closeOverlay, and the whole if (hamburger && overlay && closeBtn) listener block including the Escape keydown listener. Update the top comment block (:1-6) to drop the "Mobile hamburger overlay" bullet. The Escape + focus-return responsibility moves to Plan 02's floating-bar burger — do not re-add it here.
  </action>
  <verify>
    <automated>! grep -q 'nav-hamburger\|nav-overlay\|openOverlay\|closeOverlay' index.html css/components.css css/animations.css js/main.js && echo PASS-NO-ORPHANS</automated>
  </verify>
  <acceptance_criteria>
    - Source: `index.html` does NOT contain `class="nav-hamburger"`
    - Source: `index.html` does NOT contain `id="nav-overlay"`
    - Source: `index.html` does NOT contain `nav-overlay-link`
    - Source: `css/components.css` does NOT contain `.nav-hamburger`
    - Source: `css/components.css` does NOT contain `.nav-overlay`
    - Source: `css/animations.css` does NOT contain `.nav-overlay`
    - Source: `js/main.js` does NOT contain `openOverlay`
    - Source: `js/main.js` does NOT contain `closeOverlay`
    - Source: `js/main.js` does NOT contain `nav-hamburger`
    - Behavior: opening index.html in a browser shows no hamburger button at any viewport width
  </acceptance_criteria>
  <done>Hamburger and overlay are gone from markup, CSS, JS, and animation files; no orphaned selectors or handlers remain.</done>
</task>

<task type="auto">
  <name>Task 3: Reorder menu to Approach→Work, drop Contact, align gutter, keep mobile links</name>
  <files>index.html, css/layout.css</files>
  <read_first>
    - index.html (lines 80-98 — nav-links list)
    - css/layout.css (lines 13-37 — .nav padding + nav-links + mobile display:none)
    - css/components.css (lines 241 area — confirm hero padding-inline uses the same gutter expression)
    - .planning/phases/08-nav-floating-bar/08-CONTEXT.md (D-01, D-02, D-05, D-06)
    - .planning/phases/08-nav-floating-bar/08-PATTERNS.md (Section gutter value)
  </read_first>
  <action>
    Implements D-01, D-02, D-05, D-06. In index.html nav-links (:93-97): remove the Contact <li> entirely (D-02), and reorder the remaining items so Approach comes before Work (D-01 — match page DOM order #approach:188 then #work:200). Final list is two items: APPROACH then WORK, both still using the existing .nav-link class and #approach / #work hrefs. Keep the existing uppercase label casing already in the markup.

    In css/layout.css: delete the @media (max-width:1024px){ .nav-links{display:none} } rule (:35-37) so links stay visible on mobile down to 375px (D-06). Confirm the .nav padding (:18) uses calc(var(--space-lg) * 0.75) and that this matches the section gutter the hero and sections use (the shared expression, not a hardcoded value) so the logo sits flush-left with section content and the links align right (D-05). If the hero/section padding-inline expression differs from the nav's, reconcile both onto the single calc(var(--space-lg) * 0.75) expression. Do not introduce a hardcoded 48px.

    Verify at 375px the two links plus the logo do not overflow the header row; if they crowd, the existing gap (var(--space-md)) and flex justify-content:space-between handle layout — do not add a media query that hides links.
  </action>
  <verify>
    <automated>! grep -q 'href="#contact"' <(grep -A6 'class="nav-links"' index.html) && grep -A6 'class="nav-links"' index.html | grep -n '#approach\|#work' && ! grep -q 'nav-links { display: none' css/layout.css && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source: in the `.nav-links` list of `index.html`, the `#approach` anchor appears on an earlier line than the `#work` anchor
    - Source: the `.nav-links` list does NOT contain `href="#contact"`
    - Source: `css/layout.css` does NOT contain `.nav-links { display: none; }`
    - Source: `css/layout.css` `.nav` padding uses `calc(var(--space-lg) * 0.75)` (shared gutter expression, no hardcoded `48px`)
    - Behavior: at 375px the header shows logo + Approach + Work links inline, no hamburger, no horizontal overflow
  </acceptance_criteria>
  <done>Menu is Approach then Work with no Contact; links visible at every width; header padding on the shared section gutter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a static HTML/CSS/vanilla-JS site served by Cloudflare Pages. No backend, no database, no auth, no form, no user input. The only "boundary" is the browser rendering static markup. No data crosses any trust boundary in this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | DOM markup edits (nav/overlay removal) | accept | No untrusted input; edits are static markup. No innerHTML, no dynamic injection introduced. N/A for a static site. |
| T-08-02 | Denial of Service | none | accept | No server, no compute path. N/A. |
| T-08-03 | Information Disclosure | none | accept | No PII, no secrets, no data collection in this phase. N/A. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (no new deps — vanilla JS/CSS only, per CLAUDE.md). N/A. |

Accessibility note (not a STRIDE threat but a correctness gate): removing the overlay deletes the only Escape/focus-return handler. This plan deliberately leaves no replacement — Plan 02's burger owns that responsibility. Plan 01 introduces no keyboard trap because the header links are plain anchors with native focus behaviour.
</threat_model>

<verification>
- All orphan selectors/JS from the PATTERNS.md orphan-deletion table are gone (grep each).
- Header renders on the Hot Pink hero and scrolls off the top with the page (manual scroll check on preview).
- Menu reads Approach then Work, no Contact, at desktop and 375px.
- initStickyTab and word-roller still present and functional (sticky tab still appears past hero — it is replaced by Plan 02, but must not break in the interim).
- No console errors on page load (the deleted JS leaves no dangling references).
</verification>

<success_criteria>
- Success criteria 1 met: header non-fixed, scrolls away, scroll-driven colour animation removed (markup + JS).
- Success criteria 2 met: hamburger + overlay removed entirely (markup + JS + CSS); Contact dropped; items reordered to page order; header padding on the section gutter.
- No double-menu risk introduced: header is the only menu until Plan 02 adds the floating bar.
</success_criteria>

<output>
Create `.planning/phases/08-nav-floating-bar/08-01-SUMMARY.md` when done. Record: exact selectors/JS blocks removed, the final .nav padding expression used, any reconciliation made between nav and section gutter, and confirmation that initStickyTab + word-roller were left untouched (Plan 02 depends on this).
</output>
