---
phase: 08-nav-floating-bar
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - package.json
  - playwright.config.js
  - tests/nav-floating-bar.spec.js
  - .gitignore
autonomous: false
requirements:
  - "REFRESH-P2-PLAYWRIGHT-SUITE"
user_setup: []

must_haves:
  truths:
    - "A Playwright test harness exists and runs against the static site"
    - "Responsive snapshots are asserted at 375, 768, and 1440px"
    - "Tests assert header scrolls away, gutter alignment, menu order (Approach→Work), no Contact, no hamburger"
    - "Tests assert the floating bar is hidden over the hero and visible past it"
    - "Tests assert the mobile burger reveals Approach+Work pills sliding up and the X collapses them"
    - "Tests assert the desktop pill row (CTA + Approach + Work, no burger)"
    - "Tests assert focus trap behaviour: Escape closes and focus returns to the burger; logical tab order"
    - "Tests assert the burger line→X exists normally and is absent (no rotate transition) under reduced-motion"
  artifacts:
    - path: "tests/nav-floating-bar.spec.js"
      provides: "Heavy Playwright suite for the nav + floating bar (D-18)"
      contains: "test("
    - path: "playwright.config.js"
      provides: "Playwright config with a static web server + the three viewport projects"
    - path: "package.json"
      provides: "Dev-only @playwright/test devDependency + a test script"
  key_links:
    - from: "playwright.config.js webServer"
      to: "the static site (index.html)"
      via: "local static file server on a fixed port"
      pattern: "webServer"
---

<objective>
Stand up a Playwright test harness (none exists in the repo today) and write the heavy QA suite D-18 requires for the nav + floating-bar rework. Playwright is a dev/test tool only — it runs locally against the static files and is NOT shipped to the site (node_modules is already gitignored; the deployed bundle stays plain HTML/CSS/JS per CLAUDE.md). The suite asserts every behavioural success criterion from Plans 01 and 02 across 375 / 768 / 1440px: header scroll-away + gutter + menu order + de-burger; floating bar gate (hidden over hero, visible past it); mobile burger slide-up stack; desktop pill row; line→X present and absent under reduced-motion; focus trap + return; keyboard reachability.

Purpose: Verifiable proof that the nav rework meets all seven success criteria, runnable on demand. Locks in regression coverage for the most animation/focus-heavy surface in the project.
Output: package.json (devDependency + test script), playwright.config.js, tests/nav-floating-bar.spec.js, .gitignore entry for test artifacts.

This plan has a BLOCKING human checkpoint before the npm install (package legitimacy gate — no RESEARCH.md audit table exists for this phase, so @playwright/test is treated as [ASSUMED] and must be verified by a human before install).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-nav-floating-bar/08-CONTEXT.md
@.planning/phases/08-nav-floating-bar/08-PATTERNS.md
@.planning/phases/08-nav-floating-bar/08-01-SUMMARY.md
@.planning/phases/08-nav-floating-bar/08-02-SUMMARY.md
@./CLAUDE.md

<interfaces>
<!-- Selectors/behaviours the tests assert. Read the two SUMMARYs for the final breakpoint + class names. -->

Header (post Plan-01):
- .nav is non-fixed (scrolls away) — assert it leaves the viewport on scroll (e.g. its bounding box top goes negative / it is not in viewport after scrolling past hero)
- .nav-links contains exactly 2 links: Approach then Work; no #contact link; no .nav-hamburger anywhere
- .nav padding-inline equals the section gutter (compare computed padding to a section's computed padding-inline)

Floating bar (post Plan-02):
- .floating-bar root, toggled class .floating-bar--visible (shown past hero) / .floating-bar--suppressed (hidden at #contact)
- .floating-bar__cta — text "Free 30-min chat", href "#contact"
- .floating-bar__burger — button[aria-expanded], aria-label toggles "Open menu"/"Close menu"
- .floating-bar__burger-line — two spans
- .floating-bar__pills / .floating-bar__pills--open ; .floating-bar__pill x2 (#approach, #work)
- hero = .hero ; #contact section

Breakpoints (D-18): 375 (mobile), 768 (tablet), 1440 (desktop). Confirm the burger/pill-row switch breakpoint from 08-02-SUMMARY — desktop (1440) shows the pill row + no burger; mobile (375) shows the burger.

Serving the static site: it is plain files at repo root (index.html). Use Playwright's webServer config with a zero-dependency static server. Prefer a Node one-liner via `npx http-server` only if added as a devDependency, OR use `python3 -m http.server` (already on the machine per env — darwin) pointed at the repo root, OR Playwright's built-in `@web/dev-server`. Pick the lightest that needs no extra runtime dep beyond @playwright/test. python3 -m http.server is the safest zero-extra-dep choice; document the chosen port.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Package legitimacy gate for @playwright/test</name>
  <what-built>
    Nothing yet — this is the package legitimacy gate. Plan 03 needs the npm package `@playwright/test` (dev-only test runner). No RESEARCH.md Package Legitimacy Audit table exists for Phase 08, so per the package legitimacy protocol every package is treated as [ASSUMED] and must be human-verified before install. The project is otherwise zero-npm (CLAUDE.md), so this is the only dependency and it is dev/test-only — gitignored, never shipped to the site.
  </what-built>
  <action>
    Halt before any npm activity. Do NOT run npm install, npx playwright install, or create package.json until a human approves this gate. Present the verification steps below, wait for the resume signal, and only then proceed to Task 1. If the human chooses the manual-only alternative, skip Tasks 1 and 2 and record the override in the SUMMARY.
  </action>
  <how-to-verify>
    1. Confirm `@playwright/test` is the legitimate Microsoft-published Playwright test runner: visit https://www.npmjs.com/package/@playwright/test and check the publisher is `microsoft` / the Playwright team, weekly downloads are in the millions, and the repo links to https://github.com/microsoft/playwright.
    2. Confirm you are OK adding ONE dev-only devDependency to a project that is otherwise npm-free. It will only ever run locally for tests; it is gitignored (node_modules/) and is not part of the deployed Cloudflare Pages bundle, so the "no npm deps for V1" runtime constraint in CLAUDE.md is preserved (that rule governs the shipped site, not local test tooling).
    3. If you would rather not add any npm tooling at all, say so — the alternative is to skip automated Playwright and rely on manual verification on the Cloudflare preview (D-18 prefers the automated suite, so confirm the override explicitly).
  </how-to-verify>
  <resume-signal>Type "approved" to allow the @playwright/test install, or describe an alternative (e.g. "skip Playwright, manual only").</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Initialise the dev-only Playwright harness</name>
  <files>package.json, playwright.config.js, .gitignore</files>
  <read_first>
    - .gitignore (confirm node_modules/ already ignored)
    - ./CLAUDE.md (no-npm-for-V1 runtime constraint — this is dev-only tooling, keep it out of the shipped site)
    - .planning/phases/08-nav-floating-bar/08-02-SUMMARY.md (final breakpoint + selectors)
  </read_first>
  <action>
    After the legitimacy checkpoint is approved: create package.json at repo root declaring @playwright/test as a devDependency and a "test": "playwright test" script (plus optionally "test:ui"). Mark the package private:true so it is never published. Run the install (npm install) and install the Chromium browser binary (npx playwright install chromium) — Chromium alone is enough for this suite; do not install all browsers unless cross-browser is requested.

    Create playwright.config.js: define a webServer that serves the repo root statically on a fixed port (use `python3 -m http.server <port>` with cwd at repo root, or http-server if you prefer to add it — python3 is already present and adds no npm dep). Set baseURL to http://localhost:<port>. Define three projects/viewports for the breakpoints: mobile 375x812, tablet 768x1024, desktop 1440x900 — each using Chromium with the matching viewport. Set reuseExistingServer:true for local runs. Keep testDir = "tests".

    Add a .gitignore entry for Playwright artifacts: test-results/, playwright-report/, and the browser cache if it lands in-repo (node_modules already covered). Do NOT add any of the test tooling to the deployed paths — the Cloudflare Pages site serves index.html + css/ + js/ + fonts/ + images/ only.
  </action>
  <verify>
    <automated>test -f package.json && grep -q '@playwright/test' package.json && test -f playwright.config.js && grep -q 'webServer' playwright.config.js && npx playwright --version</automated>
  </verify>
  <acceptance_criteria>
    - Source: `package.json` exists, has `"private": true`, and lists `@playwright/test` under `devDependencies`
    - Source: `package.json` has a `test` script running `playwright test`
    - Source: `playwright.config.js` exists with a `webServer` block serving the repo root and a `baseURL` on the chosen port
    - Source: `playwright.config.js` defines the three viewports 375 / 768 / 1440
    - Source: `.gitignore` contains `test-results/` and `playwright-report/`
    - CLI: `npx playwright --version` prints a version (install succeeded)
    - Constraint: no runtime dependency added to the shipped site — `index.html` does NOT reference any node_modules path; the site still loads with only css/ + js/ + fonts/
  </acceptance_criteria>
  <done>Playwright harness installed (dev-only, gitignored), config serves the static site on a fixed port across three viewports, artifacts ignored.</done>
</task>

<task type="auto">
  <name>Task 2: Write the heavy nav + floating-bar Playwright suite</name>
  <files>tests/nav-floating-bar.spec.js</files>
  <read_first>
    - tests/ (the config from Task 1)
    - .planning/phases/08-nav-floating-bar/08-01-SUMMARY.md and 08-02-SUMMARY.md (final selectors, breakpoint, class names)
    - .planning/phases/08-nav-floating-bar/08-CONTEXT.md (D-01, D-09, D-10, D-12, D-13, D-14, D-15 — the behaviours under test)
    - index.html (the live floating-bar + nav markup)
  </read_first>
  <action>
    Implements D-18. Create tests/nav-floating-bar.spec.js covering, at the relevant viewports:

    Header (assert at 375 and 1440):
    - .nav-links has exactly 2 links; the first is Approach (#approach), the second is Work (#work); no link to #contact; no .nav-hamburger element exists; no #nav-overlay element exists.
    - Header scrolls away: after scrolling past the hero, the .nav bounding box is above the viewport (not fixed) — assert .nav is not in the viewport after scroll, or its top < 0.
    - Gutter alignment: computed padding-left of .nav equals computed padding-inline-start of a content section (e.g. #work) — within 1px.

    Floating bar gate (D-12, D-13):
    - At top of page (scrollY 0), .floating-bar does NOT have .floating-bar--visible (hidden over hero); assert it is not visible to the user.
    - After scrolling past hero height, .floating-bar has .floating-bar--visible and is visible.
    - Scroll #contact into view → .floating-bar gains .floating-bar--suppressed and is hidden.

    CTA + gradient (D-09, D-11):
    - .floating-bar__cta text is "Free 30-min chat" and href is "#contact".

    Mobile burger (375 — D-08, D-10, D-15):
    - .floating-bar__burger is visible; .floating-bar__pills are NOT shown initially.
    - Burger has exactly two .floating-bar__burger-line spans.
    - Tapping the burger sets aria-expanded="true", adds .floating-bar__pills--open, shows Approach (above) and Work (below) — assert Approach pill's box top < Work pill's box top (stacked, Approach on top per D-01/D-08), and pills sit above the burger (pills box bottom <= burger box top, allowing for gap).
    - Tapping again (the X) sets aria-expanded="false" and hides the pills.

    Desktop row (1440 — D-07):
    - .floating-bar__burger is hidden (display:none); both pills are visible as a row (Approach pill box left < Work pill box left, same row — tops roughly equal); CTA is to the left of the pill cluster.

    Focus + keyboard (375 — D-15):
    - Open the menu (click burger); focus is on the first pill (Approach).
    - Press Escape: menu closes (aria-expanded="false", pills hidden) and focus returns to the burger (assert document.activeElement is the burger).
    - Logical tab order: from the CTA, Tab reaches the burger, then (when open) the pills — assert focus moves CTA → burger in order.

    Line→X reduced-motion (D-14):
    - Default context: the .floating-bar__burger-line has a non-"none" transition (rotate animates). Assert getComputedStyle transition-duration on a burger line is > 0s, OR assert the rotate transform applies after opening with a transition present.
    - Reduced-motion context (browser.newContext({ reducedMotion: 'reduce' })): the burger line transition is "none"/0s (snaps to X, no rotate animation) and pills appear instantly (no slide transition). Assert transition-duration is 0s under reduced motion.

    Use Playwright's reducedMotion context option for the reduced-motion assertions. Where a behaviour is viewport-specific, run it only in the matching project (use test.skip on mismatched viewport or a viewport guard). Each assertion must be a real expect() — no console.log-only checks.
  </action>
  <verify>
    <automated>npx playwright test tests/nav-floating-bar.spec.js --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Source: `tests/nav-floating-bar.spec.js` exists and contains multiple `test(` blocks
    - Test command: `npx playwright test` exits 0 (all tests pass) against the built site from Plans 01 + 02
    - Coverage: tests assert menu order Approach→Work, no Contact, no hamburger (header)
    - Coverage: tests assert .nav scrolls out of viewport and gutter padding matches a section
    - Coverage: tests assert bar hidden at scrollY 0, visible past hero, suppressed at #contact
    - Coverage: tests assert CTA text "Free 30-min chat" and href "#contact"
    - Coverage: tests assert mobile burger reveals Approach-above-Work stacked above the burger, X collapses
    - Coverage: tests assert desktop pill row with burger hidden
    - Coverage: tests assert Escape closes + focus returns to burger; first-pill focus on open
    - Coverage: tests assert burger line transition present by default and 0s/none under reducedMotion:'reduce'
    - Behavior: re-running `npx playwright test` is deterministic (no flaky timing — use Playwright auto-wait / expect polling, not fixed sleeps)
  </acceptance_criteria>
  <done>The heavy D-18 suite exists and passes against the Plan 01 + 02 output, covering header, bar gate, CTA, mobile slide-up stack, desktop row, focus trap/return, keyboard order, and line→X present/absent under reduced-motion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Two boundaries enter scope in this plan, both dev-only:
1. npm install of @playwright/test — pulls third-party code onto the developer machine (supply-chain surface).
2. The local static web server Playwright spins up — binds a localhost port during test runs only.
Neither touches the deployed site: node_modules is gitignored and the Cloudflare Pages bundle is unchanged (still plain HTML/CSS/JS).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering (supply chain) | `@playwright/test` npm install | mitigate | No RESEARCH.md audit table for this phase → package treated as [ASSUMED]; BLOCKING human checkpoint before install verifies publisher (microsoft) + provenance on npmjs.com/github.com. Pinned in package.json; dev-only; gitignored; never shipped. |
| T-08-08 | Elevation of Privilege | local Playwright web server | accept | Binds localhost only, during test runs, serving already-public static files. No remote exposure, no secrets served. Negligible on a single-dev machine. |
| T-08-09 | Tampering | shipped site bundle | mitigate | Constraint enforced in acceptance criteria: index.html references no node_modules path; deployed bundle stays plain HTML/CSS/JS. The test tooling cannot leak into production. |
| T-08-10 | Information Disclosure | test artifacts (reports/screenshots) | accept | test-results/ + playwright-report/ gitignored; contain only renders of the public static site — no PII, no secrets. |

ASVS L1: the one HIGH-relevance item is supply-chain (T-08-SC), handled by the blocking legitimacy checkpoint per protocol. No other HIGH-severity threats on a static-site test harness.
</threat_model>

<verification>
- `npx playwright test` passes all tests against the Plan 01 + 02 output.
- node_modules/, test-results/, playwright-report/ are gitignored (git status shows no test tooling staged for commit beyond package.json, playwright.config.js, tests/, .gitignore).
- The deployed site is unchanged — index.html still loads only css/ + js/ + fonts/ + images/; no node_modules reference.
- Tests are deterministic across two consecutive runs (no flakiness).
</verification>

<success_criteria>
- D-18 satisfied: heavy automated suite covers responsive snapshots at 375/768/1440, header scroll-away + gutter + menu order, floating bar open/close + gate, line→X present and absent under reduced-motion, mobile slide-up stack, desktop CTA-left/nav-right row, focus trap + return, keyboard reachability.
- Test tooling is dev-only and does not violate the CLAUDE.md no-npm-for-the-shipped-site constraint.
</success_criteria>

<output>
Create `.planning/phases/08-nav-floating-bar/08-03-SUMMARY.md` when done. Record: the static-server choice + port, the Playwright version pinned, total test count + pass result, any selector/breakpoint assumptions that came from the Plan 01/02 SUMMARYs, and confirmation the shipped bundle is untouched.
</output>
