---
phase: 11-cutout-reveal-system-v1-refresh-p3
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - build.js
  - index.html
  - css/components.css
autonomous: true
requirements: [D-01, D-02, D-05, D-06, D-07, D-10]
must_haves:
  truths:
    - "node build.js produces dist/index.html with the hero SVG cutout injected (no CUTOUT marker left)"
    - "The hero shows a B&W image revealed through a window in the solid Hot Pink section colour (D-01/D-02)"
    - "The hero <head> preloads the hero image so LCP is preserved after the <img>→SVG swap (D-06)"
    - "The dead rewriteHeroImg function is removed; build.js calls buildCutout at the Phase 11 seam (D-10)"
    - "No scroll animation on the cutout windows — static render only (D-07)"
  artifacts:
    - path: "build.js"
      provides: "buildCutout wired at the Phase 11 seam; rewriteHeroImg removed; buildCutout.js watched"
      contains: "await buildCutout(manifest)"
    - path: "index.html"
      provides: "Hero marker + preload link replacing the old <picture class=hero__cutout> block"
      contains: "<!-- CUTOUT:hero -->"
    - path: "css/components.css"
      provides: ".cutout primitive class; .hero__cutout grayscale/radius rules removed"
      contains: ".cutout"
  key_links:
    - from: "build.js"
      to: "buildCutout"
      via: "require + await at the Phase 11 seam (between buildImages and buildHtml)"
      pattern: "await buildCutout\\(manifest\\)"
    - from: "index.html <head>"
      to: "hero image"
      via: "link rel=preload as=image"
      pattern: "rel=\"preload\"[^>]*as=\"image\""
    - from: "dist/index.html hero"
      to: "scene-cafe webp"
      via: "generated SVG <image href>"
      pattern: "scene-cafe-960\\.webp"
---

<objective>
Wire `buildCutout` into the Phase 12 build, swap the hero markup to the injection marker + a preload link, and refactor the hero CSS onto the `.cutout` primitive. After this plan, `node build.js` emits a `dist/index.html` whose hero is the generated SVG cutout: a B&W image revealed through a window in the solid Hot Pink field (D-01/D-02/D-03/D-05).

Purpose: Prove the primitive on the real hero surface (D-05) inside the established build pipeline (D-10), with no LCP/CLS regression and no scroll animation (D-06/D-07).
Output: edited `build.js` (seam + dead-code removal + watch target), edited `index.html` (marker + preload), edited `css/components.css` (.cutout primitive, old rules removed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cutout-reveal-system-v1-refresh-p3/11-CONTEXT.md
@.planning/phases/11-cutout-reveal-system-v1-refresh-p3/11-RESEARCH.md
@.planning/phases/11-cutout-reveal-system-v1-refresh-p3/11-PATTERNS.md
@.planning/phases/11-cutout-reveal-system-v1-refresh-p3/11-01-SUMMARY.md

<interfaces>
<!-- buildCutout export from Plan 01 -->
const { buildCutout } = require('./buildCutout');
// async buildCutout(manifest) -> manifest  (reads + rewrites dist/index.html)

<!-- build.js seam (lines 296-304): insert `manifest = await buildCutout(manifest);` -->
<!-- build.js rewriteHeroImg: function at lines 202-236, called at line 185 — REMOVE both -->
<!-- build.js WATCH_TARGETS: array at lines 327-333 — add buildCutout.js -->

<!-- index.html current hero block (lines 112-122): <div class="hero__cutouts"><picture class="hero__cutout"><img .../></picture></div> -->
<!-- index.html existing preload pattern (line 26): <link rel="preload" href="/fonts/epilogue-400.woff2" as="font" ...> -->

<!-- css/components.css hero cutout rules: lines 228-264 (.hero__cutouts, .hero__cutout, .hero__cutout img, @media 768) -->
<!-- --radius-cutout token = 16px (css/tokens.css:81) — now expressed in the SVG rounded-rect shape, not CSS -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire buildCutout at the seam and remove dead rewriteHeroImg</name>
  <read_first>
    - build.js lines 1-27 (header + constants), 176-236 (buildHtml + rewriteHeroImg), 288-353 (orchestrator + watch)
    - buildCutout.js (Plan 01 output — the export being wired)
    - 11-RESEARCH.md (Pattern 2 seam location, Pitfall 2 + Pitfall 5, Open Question 3 remove rewriteHeroImg)
    - 11-PATTERNS.md (`build.js` seam integration section: require at top, call at seam, rewriteHeroImg removal target, WATCH_TARGETS)
  </read_first>
  <files>build.js</files>
  <action>
Add `const { buildCutout } = require('./buildCutout');` near the other requires at the top of build.js.

At the Phase 11 seam in `build()` (the comment block at lines 298-302, between `const manifest = await buildImages();` and `buildHtml(manifest);`), insert `manifest = await buildCutout(manifest);`. Note `buildCutout` runs AFTER `buildHtml` writes dist/index.html — so move the seam call to after `buildHtml(manifest);` (buildCutout reads and rewrites the already-written dist/index.html per 11-PATTERNS.md dist/ read-then-write). Final order: `buildHtml(manifest)` → `manifest = await buildCutout(manifest)` → `copyStatic()`.

Remove the dead `rewriteHeroImg` (Pitfall 2 / Open Question 3): delete the function body (lines 202-236) and change the call site in `buildHtml` (line 185) from `const html = rewriteHeroImg(srcHtml, manifest);` to write `srcHtml` straight through. Drop the now-unused `manifest` param from `buildHtml` for cleanliness, and update its log line to `[build] HTML written → dist/index.html`. Update the call in `build()` to `buildHtml()`.

Add `path.join(ROOT, 'buildCutout.js')` to the `WATCH_TARGETS` array (Pitfall 5 — shape-config edits trigger rebuild).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -c "rewriteHeroImg" build.js</automated>
  </verify>
  <acceptance_criteria>
    - `node build.js` exits 0 and prints `[buildCutout] injected cutout: hero`.
    - `grep -c "rewriteHeroImg" build.js` returns 0 (dead function fully removed).
    - `grep -c "await buildCutout(manifest)" build.js` returns 1, placed after the `buildHtml` call.
    - `grep "buildCutout.js" build.js` shows it in WATCH_TARGETS.
    - `grep -c "data:image" dist/index.html` returns 0 (D-06 — no base64).
  </acceptance_criteria>
  <done>build.js requires buildCutout, calls it after buildHtml at the Phase 11 seam, has no remaining rewriteHeroImg references, and watches buildCutout.js. `npm run build` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Swap hero markup to the cutout marker + add hero image preload</name>
  <read_first>
    - index.html lines 20-32 (head preloads), lines 89-124 (hero section + current hero__cutouts block)
    - 11-RESEARCH.md (Pattern 6 marker, Pitfall 4 preload link)
    - 11-PATTERNS.md (`index.html` section: hero block replacement, <head> preload pattern)
  </read_first>
  <files>index.html</files>
  <action>
Replace the inner `<picture class="hero__cutout">...</picture>` block (lines 113-121) with the literal marker `<!-- CUTOUT:hero -->`. Keep the `<div class="hero__cutouts">` wrapper (renaming is optional churn — keep the name). Result: `<div class="hero__cutouts"><!-- CUTOUT:hero --></div>`. The old `<img>` and its alt/width/height/loading/fetchpriority attributes go away — those responsibilities move into the generated SVG (D-05).

In `<head>`, immediately after the existing font preload (line 26), add the hero image preload: `<link rel="preload" href="/images/scene-cafe-960.webp" as="image" fetchpriority="high">`. This is the cross-browser high-priority signal for the LCP image now that the hero `<img>` is replaced by an SVG `<image>` (Pitfall 4 — fetchpriority on SVG `<image>` is not reliably honoured). Commit it directly in source (simpler than a PRELOAD marker per Pattern 6).

Do NOT add any animation/reveal classes to the hero cutout (D-07 static render). The hero `<h1>` stays exactly as is (one H1 per page).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && node -e "const fs=require('node:fs'); const s=fs.readFileSync('index.html','utf8'); if(!s.includes('<!-- CUTOUT:hero -->')) throw new Error('marker missing'); if(!/rel=\"preload\"[^>]*as=\"image\"/.test(s)) throw new Error('preload missing'); const d=fs.readFileSync('dist/index.html','utf8'); if(d.includes('<!-- CUTOUT:hero -->')) throw new Error('marker not replaced in dist'); if(!d.includes('<svg class=\"cutout')) throw new Error('no svg in dist'); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Source `index.html` contains `<!-- CUTOUT:hero -->` and no `<picture class="hero__cutout">`.
    - Source `<head>` contains `<link rel="preload" ... as="image" ...>` for scene-cafe (D-06 LCP guard).
    - After build, `dist/index.html` contains `<svg class="cutout` and `<feColorMatrix` and does NOT contain the `<!-- CUTOUT:hero -->` marker.
    - `grep -c "<h1" index.html` returns 1 (one H1 preserved).
    - No `data-reveal` / animation attribute on the cutout (D-07): `grep -c "CUTOUT:hero" index.html` returns 1 and that line carries no reveal class.
  </acceptance_criteria>
  <done>Source index.html has the hero marker + image preload; the old picture block is gone; the built dist/index.html has the SVG cutout injected with the marker consumed.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor hero CSS onto the .cutout primitive</name>
  <read_first>
    - css/components.css lines 228-264 (current .hero__cutouts / .hero__cutout / @media rules)
    - css/tokens.css line 81 (--radius-cutout — now expressed in the SVG shape, not CSS)
    - 11-RESEARCH.md (Pattern 4 responsive scaling, Pitfall 3 CLS, Pitfall 6 stale CSS rules)
    - 11-PATTERNS.md (`css/components.css` replacement pattern + section-comment fence style)
  </read_first>
  <files>css/components.css</files>
  <action>
Replace the cutout rules at lines 228-264. Add a new `.cutout` primitive class (`display: block; width: 100%; height: auto;`) under a `/* === Cutout primitive (.cutout) === */` fence using the existing section-comment style. Keep `.hero__cutouts` as a thin positioning wrapper (`width: 100%; max-width: 460px; justify-self: end;`) and its `@media (max-width: 768px)` rules (80% width, `margin-block-start: var(--space-md)`, `max-width: none`, `justify-self: center`).

REMOVE entirely: `.hero__cutout` (the `filter: grayscale(100%)` + `border-radius` + `position: absolute; inset: 0` rule) and `.hero__cutout img` — grayscale now lives in the SVG feColorMatrix (D-03) and the corner radius lives in the rounded-rect shape path (Pitfall 6). REMOVE `aspect-ratio: 4 / 5` from `.hero__cutouts` — aspect ratio is now intrinsic from the SVG `width`/`height` attributes, which is the CLS guard (Pitfall 3). Do not add any gradient, shadow, or drop-shadow filter (CLAUDE.md design bans; D-01/D-02 keep the field as the solid section colour).
  </action>
  <verify>
    <automated>npm run build && npx playwright test tests/build-smoke.spec.js --project=desktop-1440 2>&1 | tail -5 && grep -c "hero__cutout " css/components.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.cutout" css/components.css` returns >= 1 (new primitive class present).
    - `grep -c "filter: grayscale" css/components.css` returns 0 (moved to SVG feColorMatrix, D-03).
    - `grep -c "aspect-ratio" css/components.css` for the .hero__cutouts block returns 0 (CLS now from SVG intrinsic dims, Pitfall 3).
    - No `.hero__cutout {` or `.hero__cutout img` rules remain: `grep -c "\.hero__cutout " css/components.css` returns 0.
    - `npm run build` exits 0 and the existing build-smoke spec still passes.
  </acceptance_criteria>
  <done>css/components.css has a `.cutout` primitive, a thinned `.hero__cutouts` wrapper with no aspect-ratio rule, and no `.hero__cutout` grayscale/radius rules. Build succeeds and build-smoke passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source index.html → dist/index.html | buildCutout replaces a build-controlled marker with a build-generated SVG; the marker and SVG are both internal build artifacts |
| build pipeline → served static HTML | dist/ is the deploy artifact; no server runtime |

Static marketing site, build-time only. No auth, no runtime user input touched here. The Formspree contact form (the only runtime input) is untouched by this phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | source→dist marker replacement | mitigate | The `<!-- CUTOUT:hero -->` marker is a static string in committed source; buildCutout replaces it with SVG built only from controlled inputs. Build-smoke asserts the marker is consumed (not left in output) and no stray markup leaks. |
| T-11-05 | Denial of Service | LCP regression after <img>→SVG swap | mitigate | Add `<link rel="preload" as="image">` for the hero image (Pitfall 4). CLS guarded by SVG intrinsic width/height (Pitfall 3). Verified by Plan 03 build-output assertions + post-deploy Lighthouse. |
| T-11-06 | Tampering | dead rewriteHeroImg left in build.js | mitigate | Remove the function entirely so no stale regex silently no-ops on future hero markup changes (Open Question 3 / Pitfall 2). |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (no new deps — RESEARCH audit: none). Nothing to verify. |
</threat_model>

<verification>
- `npm run build` exits 0 and prints `[buildCutout] injected cutout: hero`.
- `dist/index.html` contains the injected SVG cutout, no leftover marker, no base64.
- Source `<head>` carries the hero image preload (LCP guard).
- `css/components.css` has the `.cutout` primitive; no grayscale/aspect-ratio/`.hero__cutout` rules remain.
- Existing build-smoke spec still passes.
</verification>

<success_criteria>
- build.js wires buildCutout at the seam (after buildHtml), removes rewriteHeroImg, watches buildCutout.js (D-10).
- Hero markup is the marker + preload; the old picture block is gone (D-05, D-06).
- CSS refactored onto `.cutout`; grayscale + radius moved into the SVG (D-03); CLS guarded by SVG intrinsic dims.
- Field stays the solid section colour — no gradient/shadow added (D-01/D-02).
- Static render — no animation on the cutout (D-07).
</success_criteria>

<output>
Create `.planning/phases/11-cutout-reveal-system-v1-refresh-p3/11-02-SUMMARY.md` when done.
</output>
