---
phase: 11-cutout-reveal-system-v1-refresh-p3
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - tests/cutout.spec.js
  - CLAUDE.md
autonomous: true
requirements: [D-03, D-06, D-07, D-09, D-10a]
must_haves:
  truths:
    - "A Playwright spec proves the hero cutout renders, is decorative, carries no base64, and the CUTOUT marker is consumed (D-09)"
    - "The spec asserts the SVG has intrinsic width/height (CLS guard) and the head preloads the hero image (LCP guard, D-06)"
    - "The spec asserts grayscale is via SVG feColorMatrix, not CSS filter (D-03)"
    - "CLAUDE.md records the cutout primitive as build-time codegen emitting static SVG (D-10a doc note)"
  artifacts:
    - path: "tests/cutout.spec.js"
      provides: "Build-output + browser-render assertions for the cutout primitive"
      contains: "test.describe"
    - path: "CLAUDE.md"
      provides: "Cutout primitive note: build-time SVG codegen, source→output marker model"
      contains: "buildCutout"
  key_links:
    - from: "tests/cutout.spec.js"
      to: "dist/index.html"
      via: "fs.readFileSync assertions on generated SVG"
      pattern: "cutout-windows-hero"
    - from: "tests/cutout.spec.js"
      to: "rendered page"
      via: "page.goto('/') + locator on svg.cutout"
      pattern: "svg\\.cutout"
---

<objective>
Lock the cutout primitive behind a Playwright spec and document the build-time codegen model in CLAUDE.md. The spec proves the contract from both sides: build-output assertions (read dist/index.html via fs — same pattern as build-smoke) and browser-render assertions (page renders the SVG, it is decorative, no base64, CLS/LCP guards present).

Purpose: Make the primitive regression-proof and record the source→output split for the next maintainer (D-10a doc note — the CLAUDE.md tech-stack relaxation was already recorded 2026-06-02; this adds the cutout-specific build-model note).
Output: `tests/cutout.spec.js` (new) and a short CLAUDE.md note on the cutout build model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cutout-reveal-system-v1-refresh-p3/11-CONTEXT.md
@.planning/phases/11-cutout-reveal-system-v1-refresh-p3/11-RESEARCH.md
@.planning/phases/11-cutout-reveal-system-v1-refresh-p3/11-PATTERNS.md
@.planning/phases/11-cutout-reveal-system-v1-refresh-p3/11-02-SUMMARY.md
@tests/build-smoke.spec.js
@tests/nav-floating-bar.spec.js

<interfaces>
<!-- build-smoke.spec.js header pattern (lines 1-33): @ts-check, 'use strict', require @playwright/test + node:fs/path, ROOT/DIST consts. Copy verbatim. -->
<!-- fs-based assertion pattern: fs.readFileSync(path.join(DIST,'index.html')) then expect(html).toContain(...) -->
<!-- browser-render pattern (nav-floating-bar.spec.js): page.goto('/'), page.locator(...), expect(...).toBeVisible() -->

<!-- Generated SVG identifiers to assert against (from Plan 01/02 output): -->
<!--   <svg class="cutout cutout--hero" ... aria-hidden="true" role="presentation" -->
<!--   <mask id="cutout-windows-hero" ...>  <filter id="cutout-grayscale-hero" ...><feColorMatrix type="saturate" values="0"/> -->
<!--   <image href="/images/scene-cafe-960.webp" ... mask="url(#cutout-windows-hero)" filter="url(#cutout-grayscale-hero)"/> -->
<!--   width="1000" height="1064" on the <svg> (CLS guard) -->
<!-- playwright.config.js: webServer builds dist/ then serves on 7777; three viewport projects. No config change needed. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/cutout.spec.js (build-output + browser-render)</name>
  <read_first>
    - tests/build-smoke.spec.js (full file — header, fs-based assertion pattern, no-server tests)
    - tests/nav-floating-bar.spec.js lines 1-60 (browser-render pattern: page.goto, locator, scroll helper)
    - dist/index.html (the generated output from Plan 02 — confirm the exact emitted identifiers)
    - 11-PATTERNS.md (`tests/cutout.spec.js` section: both spec categories + the exact assertion examples)
    - 11-RESEARCH.md (Pitfall 1 unique IDs, Pitfall 3 CLS, Pitfall 4 preload, D-09 single image)
  </read_first>
  <files>tests/cutout.spec.js</files>
  <behavior>
    - Build-output (no browser, fs.readFileSync dist/index.html): asserts `<svg class="cutout`, `<mask id="cutout-windows-hero"`, `<filter id="cutout-grayscale-hero"`, `<feColorMatrix`, `scene-cafe-960.webp`; asserts the `<!-- CUTOUT:hero -->` marker is NOT present; asserts NO `data:image/` base64; asserts exactly one `<image` inside the cutout svg (D-09); asserts the svg carries `width="1000"` and `height="1064"` (CLS guard); asserts the head has a `rel="preload"` `as="image"` for the hero image (LCP guard).
    - Browser-render (page.goto('/')): `svg.cutout` is visible in the hero; the svg has `aria-hidden="true"`; page source has no `data:image/`.
  </behavior>
  <action>
Create `tests/cutout.spec.js`. Copy the build-smoke header verbatim (`// @ts-check`, JSDoc usage line, `'use strict';`, requires, `ROOT`/`DIST` consts). Per 11-PATTERNS.md, author two `test.describe` groups:

`test.describe('Cutout build output', ...)` — fs-only tests reading `dist/index.html` (no server needed): assert it contains `<svg class="cutout`, `<mask id="cutout-windows-hero"`, `<filter id="cutout-grayscale-hero"`, `<feColorMatrix`, `scene-cafe-960.webp`; assert it does NOT contain `<!-- CUTOUT:hero -->` (marker consumed) nor `data:image/` (D-06 no base64); assert exactly one `<image` inside the cutout SVG (D-09 single shared image) via a count; assert `width="1000"` and `height="1064"` on the svg (Pitfall 3 CLS guard); assert the `<head>` contains a `rel="preload"` with `as="image"` referencing the hero image (Pitfall 4 LCP guard).

`test.describe('Cutout hero render', ...)` — browser tests: `page.goto('/')`, locate `.hero__cutouts svg.cutout`, assert `toBeVisible()`; assert the svg `toHaveAttribute('aria-hidden', 'true')` (decorative, D-09 alt=''); fetch the page HTML and assert it has no `data:image/`.

Do NOT test scroll animation — D-07 is static render; there is nothing to animate. Use the three existing viewport projects as-is (no playwright.config.js change).
  </action>
  <verify>
    <automated>npx playwright test tests/cutout.spec.js 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/cutout.spec.js` exits 0 across all three viewport projects.
    - The spec file contains assertions for: `cutout-windows-hero`, `feColorMatrix`, `scene-cafe-960.webp`, marker-absence, no-base64, intrinsic width/height, and the preload link.
    - `grep -c "test.describe" tests/cutout.spec.js` returns >= 2 (build-output + browser-render groups).
    - The spec asserts exactly one `<image` in the cutout SVG (D-09).
  </acceptance_criteria>
  <done>tests/cutout.spec.js exists and passes; it proves the cutout build output (SVG, mask, grayscale filter, manifest href, no marker, no base64, CLS + LCP guards) and the browser render (visible, decorative).</done>
</task>

<task type="auto">
  <name>Task 2: Document the cutout build model in CLAUDE.md</name>
  <read_first>
    - CLAUDE.md (the `## Project` Constraints block on tech stack/build, and the `## Local preview server` note for the carve-out wording style to mirror)
    - 11-CONTEXT.md (D-10, D-10a — build-time codegen emitting static HTML; the relaxation already recorded 2026-06-02)
    - dist/index.html (the generated hero SVG, to describe the source→output split accurately)
  </read_first>
  <files>CLAUDE.md</files>
  <action>
Add a short `## Cutout primitive (build-time SVG codegen)` note to CLAUDE.md, mirroring the wording style of the existing "Local preview server" / contact-form carve-out notes. State plainly: the hero (and future section) cutouts are generated at build time by `buildCutout.js`; source `index.html` carries a `<!-- CUTOUT:hero -->` marker that `node build.js` replaces with an inline `<svg>` in `dist/index.html`; the field behind the windows is the solid section colour (D-01/D-02 — brand gradient stays only on the floating CTA pill); revealed imagery is B&W via SVG `feColorMatrix` (D-03); all windows share one `<image>` behind one `<mask>` (D-09); the shipped artifact stays plain static SVG/HTML, no client-side runtime, no base64 (D-06). Note the five shape presets live in `buildCutout.js` `SHAPE_PRESETS` and that future sections (refresh P5 Services, P8 Visual variety) add their own `CUTOUT_CONFIGS` entries.

Do NOT re-add or relax any design ban. The tech-stack relaxation itself is already recorded (2026-06-02) — this note is the cutout-specific build-model record (D-10a), not a new reversal.
  </action>
  <verify>
    <automated>grep -c "buildCutout\|CUTOUT:hero\|build-time SVG" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "buildCutout" CLAUDE.md` returns >= 1.
    - CLAUDE.md describes the source→output marker model (`<!-- CUTOUT:hero -->` replaced at build).
    - The note states the field is the solid section colour (D-01/D-02) and imagery is B&W via feColorMatrix (D-03).
    - No design ban is removed or weakened: `grep -c "no card shadows\|no gradient text\|Epilogue" CLAUDE.md` is unchanged (>= 1).
  </acceptance_criteria>
  <done>CLAUDE.md carries a cutout-primitive build-model note covering the marker replacement, solid-colour field, B&W feColorMatrix, single shared image, and static-output guarantee — mirroring the existing carve-out note style.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dist/index.html → test assertions | Tests read the build artifact; no untrusted input |
| CLAUDE.md edit | Documentation only; no runtime/build behaviour change |

Static marketing site, build-time only. This plan adds a test and a doc note — no new code paths, no runtime input. The Formspree contact form is untouched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering | regression in cutout output | mitigate | cutout.spec.js asserts the generated SVG structure, no leftover marker, and no base64 — a future build that breaks the codegen or reintroduces base64 fails CI. |
| T-11-08 | Information Disclosure | base64 image bloat / data leak in output | mitigate | Both fs-based and browser tests assert no `data:image/` reaches the served HTML (D-06). |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (no new deps — RESEARCH audit: none). Nothing to verify. |
</threat_model>

<verification>
- `npx playwright test tests/cutout.spec.js` exits 0 on all three viewports.
- The spec covers SVG structure, marker-absence, no-base64, single image, CLS + LCP guards, and decorative aria.
- CLAUDE.md documents the cutout build model without weakening any design ban.
</verification>

<success_criteria>
- tests/cutout.spec.js proves the primitive from build-output and browser-render sides (D-03, D-06, D-09).
- No scroll-animation test exists — static render is the contract (D-07).
- CLAUDE.md records the build-time codegen model and source→output marker split (D-10a doc note).
- Phase-wide: hero now renders the SVG cutout; full `npm test` passes (cutout + build-smoke + nav + contact-form).
</success_criteria>

<output>
Create `.planning/phases/11-cutout-reveal-system-v1-refresh-p3/11-03-SUMMARY.md` when done.
</output>
