---
phase: 14-define-the-offer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/OFFER.md
  - .planning/STATE.md
autonomous: true
requirements: []   # No REQ-IDs map to this phase. Coverage driven by ROADMAP §"Phase 14" success criteria 1-3 and CONTEXT decisions D-01..D-10.

must_haves:
  truths:
    - "Each rung (Diagnose, Strategy, Embed) is a named package with a price (or price model) and a one-line outcome — ROADMAP success criterion #1, D-02/D-04/D-05/D-10"
    - "The productised vs bespoke split is readable per rung: Diagnose=productised/fixed, Strategy=semi-productised/scoped, Embed=bespoke/retainer — ROADMAP success criterion #2, D-06"
    - "OFFER.md is structured so Services (15), Process (16), About (17), Intro (19) can each reference a defined offer — ROADMAP success criterion #3"
    - "The free 30-min call is the single primary CTA and entry point, with no-sale/no-follow-up reassurance, and is explicitly NOT a priced rung — D-07/D-08"
    - "Every package CTA routes to the free call / Formspree contact form, no direct buy/booking — D-09"
    - "The three disciplines appear as 'what this draws on' inside rungs, never as separate purchasable tiles — D-03"
    - "Every provisional price, public name, one-line outcome, and what's-included list carries [CONFIRM-KRIS] — D-05 + Claude's-discretion items"
    - "The offer reads as a commitment ladder, not a Good/Better/Best SaaS pricing table — CONTEXT anti-reference"
    - "RESEARCH.md's three open questions (rung vocabulary, Embed anchor £2,500 vs £3,000/month, actual Diagnostic process) are carried into the spec as flagged open items"
  artifacts:
    - path: ".planning/OFFER.md"
      provides: "The locked offer spine — content contract consumed by phases 15/16/17/19"
      contains: "## Rung 1"
    - path: ".planning/STATE.md"
      provides: "Pointer to .planning/OFFER.md so downstream phases find the artifact by stable path"
      contains: "OFFER.md"
  key_links:
    - from: ".planning/OFFER.md"
      to: "#contact (Formspree form)"
      via: "every rung CTA + free-call CTA route here"
      pattern: "contact|Formspree"
    - from: ".planning/STATE.md"
      to: ".planning/OFFER.md"
      via: "recorded artifact path"
      pattern: "OFFER\\.md"
---

<objective>
Author `.planning/OFFER.md` — the locked offer spine. It defines, as named priced packages, the three-rung commitment ladder (Diagnose → Strategy → Embed) plus the free-call entry point, translating the locked CONTEXT decisions (D-01..D-10) and the RESEARCH draft content into a single downstream-consumable spec.

Purpose: This is the highest-leverage decision in the V1 restructure. Phase 15 renders it as a priced comparison; Phases 16/17/19 reference its rungs and outcomes. Every later content phase reads this file before building.

Output: `.planning/OFFER.md` (the spec) + a one-line pointer in `.planning/STATE.md` so downstream phases find it by stable path.

This phase produces NO production code (no HTML/CSS/JS). It is a content/strategy spec only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the offer spine — .planning/OFFER.md</name>
  <read_first>
    - .planning/phases/14-define-the-offer/14-CONTEXT.md — THE LOCKED DECISIONS D-01..D-10. Authoritative. Read first, do not re-decide structure.
    - .planning/phases/14-define-the-offer/14-RESEARCH.md — the OFFER.md field template (§"Artifact Shape", lines ~284-342), the provisional draft "What's Included" lists (lines ~211-244), provisional public rung names (§"Rung Naming Conventions", lines ~196-201), pricing verdicts, the free-call vs Diagnose distinction, the five pitfalls, and the three Open Questions (lines ~400-416).
    - PRODUCT.md — audience, five trigger situations, brand voice, design principle #2 ("name the problem before the solution"). The offer language must respect this voice.
    - .planning/seeds/CONTENT-DRAFT.md §"Section 5: Services" and §"Section 6" (free-session copy) — directional source for the existing three-discipline framing the ladder supersedes, and the established free-call reassurance line.
    - CLAUDE.md §"Contact form: live-domain-only submission" — every CTA routes to the single Formspree #contact form; no mailto, no direct booking. Also the copy rules: no em-dashes, no filler words.
  </read_first>
  <files>.planning/OFFER.md</files>
  <action>
Create `.planning/OFFER.md` following the exact section template in RESEARCH.md §"Artifact Shape" (lines ~284-342). Do NOT paste RESEARCH wholesale — translate it into a clean spec. Required sections, in order:

1. `# The Offer: Look Twice` then `## Overview` — two sentences: the ladder is a three-rung commitment journey (Diagnose → Strategy → Embed); the way in is a free 30-min call; not every client moves through all three. State explicitly it is a commitment ladder, NOT a Good/Better/Best tier choice (CONTEXT anti-reference, D-01).

2. `## Free Call (Entry Point — not a rung)` — covers: what it is (surface-level problem-naming, qualification, no pitch); duration 30 minutes; the reassurance line "No sale, no follow-up unless you want one." (the established Phase 7 / CLAUDE.md wording — carry it verbatim, mark `[CONFIRM-KRIS]`); routes into Diagnose, or straight to Strategy/Embed when fit is obvious; contact route = Formspree form at #contact. State plainly this is the single primary CTA site-wide and is NOT a priced rung — Diagnose is the first paid thing (D-07/D-08). Add a one-line note distinguishing it from the paid Diagnose: free call = a 30-min conversation, surface-level; Diagnose = deep investigation with a written output (RESEARCH Pitfall 1 + 3).

3. `## Rung 1` — internal label Diagnose. Fields (use bold labels as in the RESEARCH template): public name (provisional "The Diagnostic" `[CONFIRM-KRIS]`); Type = Productised — fixed scope, fixed price; Price = ~£2,000 fixed `[CONFIRM-KRIS]`; Duration ~2-3 weeks `[CONFIRM-KRIS]`; one-line outcome = D-10 Diagnose draft verbatim, marked `[CONFIRM-KRIS]`; What's included = the 3-5 draft bullets from RESEARCH lines ~215-221 (these MUST include investigation activities — document review, stakeholder interviews, research — not only calls, per Pitfall 1), marked `[CONFIRM-KRIS]`; Disciplines drawn on = brand strategy, CX/experience strategy, research & insight (as "what this draws on", D-03); CTA = routes to free call / Formspree #contact form (D-09).

4. `## Rung 2` — internal label Strategy. Public name provisional "Strategy Sprint" `[CONFIRM-KRIS]`; Type = Semi-productised — fixed shape, scoped per client; Price = from ~£6,000 `[CONFIRM-KRIS]`; Duration ~4-8 weeks `[CONFIRM-KRIS]`; one-line outcome = D-10 Strategy draft verbatim `[CONFIRM-KRIS]`; What's included = RESEARCH lines ~225-231 `[CONFIRM-KRIS]`; Disciplines drawn on (D-03); CTA routes to free call / #contact (D-09); plus the scope note (scope confirmed and priced in the free call; Diagnose often precedes but is not required).

5. `## Rung 3` — internal label Embed. Public name provisional "Embedded Partner" `[CONFIRM-KRIS]`; Type = Bespoke — tailored retainer; Price = from ~£2,500 / month `[CONFIRM-KRIS]`; Duration = rolling, reviewed quarterly; one-line outcome = D-10 Embed draft verbatim `[CONFIRM-KRIS]`; What's included = RESEARCH lines ~236-242 framed around outcomes/availability NOT hours (Pitfall 5), `[CONFIRM-KRIS]`; Disciplines drawn on = all three, continuously (D-03); CTA routes to free call / #contact (D-09).

6. `## Not Cumulative Tiers` (short note) — state explicitly: these are not cumulative tiers; each rung is a different kind of engagement for a different stage; not every client moves through all three (RESEARCH Pitfall 2). This enforces the commitment-ladder framing for Phase 15.

7. `## Downstream Consumer Notes` — one bullet each for Phase 15 (renders priced comparison; needs name/price/outcome/included per rung), 16 (references Diagnose + Strategy as journey steps), 17 (references Embed as the ongoing partnership), 19 (CTA references the free-call entry, not a specific rung). Per RESEARCH anti-pattern: do NOT specify Phase 15 layout here.

8. `## Open Items for Kris` — carry RESEARCH.md's three Open Questions (lines ~402-416) verbatim-in-substance: (a) does Kris have her own client vocabulary for the rung names; (b) should the Embed anchor move up to ~£3,000/month (mid-market equivalent); (c) what exactly her Diagnostic process involves vs a thorough free call. Each flagged for Kris.

9. `## Confirmation Required Before Launch` — one line: all `[CONFIRM-KRIS]` items need Kris's sign-off before any copy goes live.

Copy rules throughout (CLAUDE.md): no em-dashes (hyphens in number-word compounds like "30-min", "2-3 weeks" are fine); no filler words (just, really, basically, simply); short plain sentences in Kris's voice; no consultant waffle. Final voice polish is Phase 20 — draft only here.
  </action>
  <acceptance_criteria>
    - OFFER.md exists at .planning/OFFER.md with H1 "# The Offer: Look Twice".
    - File contains a `## Free Call` section that states it is the single primary CTA / entry point AND is not a priced rung, AND contains the "No sale, no follow-up" reassurance line.
    - File contains exactly three rung sections, one each for internal labels Diagnose, Strategy, Embed (in that order).
    - Each rung section contains all of: public name, Type (fixed/scoped/bespoke wording), Price (or price model), Duration, one-line outcome, What's included (3-5 bullets), Disciplines drawn on, CTA. Verify with: `grep -c "What's included" .planning/OFFER.md` returns 3.
    - Each rung Type line makes the productised/semi-productised/bespoke split readable: grep finds "Productised" (Diagnose), "Semi-productised" (Strategy), "Bespoke" (Embed).
    - Every price figure (~£2,000, ~£6,000, ~£2,500), every provisional public name, and every one-line outcome is followed by a [CONFIRM-KRIS] marker. Verify count: `grep -c "CONFIRM-KRIS" .planning/OFFER.md` is at least 9.
    - The three disciplines (brand strategy, CX/experience strategy, research & insight) appear as "what this draws on" inside rungs, NOT as standalone purchasable sections (no top-level `## Brand strategy` / `## Research` rung headings).
    - Every rung CTA and the free-call CTA route to the free call / Formspree #contact form. Verify: `grep -i "contact" .planning/OFFER.md` shows each CTA points there; no "buy now" / "book now" / direct-booking language present.
    - A "Downstream Consumer Notes" section names Phase 15, 16, 17 and 19.
    - An "Open Items for Kris" section carries all three RESEARCH Open Questions (rung vocabulary; Embed anchor £2,500 vs £3,000/month; actual Diagnostic process).
    - No em-dash characters present in the file body copy. Verify: file contains no U+2014 in draft copy.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/OFFER.md && [ "$(grep -c "What's included" .planning/OFFER.md)" = "3" ] && [ "$(grep -c "CONFIRM-KRIS" .planning/OFFER.md)" -ge 9 ] && grep -q "Productised" .planning/OFFER.md && grep -q "Semi-productised" .planning/OFFER.md && grep -q "Bespoke" .planning/OFFER.md && grep -qi "no sale, no follow-up" .planning/OFFER.md && ! grep -q $'—' .planning/OFFER.md && echo PASS</automated>
  </verify>
  <done>OFFER.md exists with overview, free-call entry, three fully-specified rungs (Diagnose/Strategy/Embed), not-cumulative note, downstream consumer notes, open items, and confirmation note. All three ROADMAP success criteria are satisfiable from the file. Every provisional value carries [CONFIRM-KRIS]. No em-dashes.</done>
</task>

<task type="auto">
  <name>Task 2: Record the OFFER.md pointer in STATE.md and self-verify against the three success criteria</name>
  <read_first>
    - .planning/STATE.md — current project state; find where artifacts/decisions are recorded so the pointer lands in the right place (do not duplicate an existing entry).
    - .planning/OFFER.md — the file written in Task 1, to self-verify its contents satisfy the three ROADMAP success criteria.
    - .planning/ROADMAP.md §"Phase 14" — the three success criteria the file must satisfy (lines ~297-302).
  </read_first>
  <files>.planning/STATE.md</files>
  <action>
Add a single pointer entry to `.planning/STATE.md` recording that the locked offer spine lives at `.planning/OFFER.md` (stable path), with a one-line note that it is the content contract consumed by Phases 15/16/17/19 and that all `[CONFIRM-KRIS]` items await Kris's sign-off. Place it where STATE.md records artifacts or current-phase outputs — match the existing format, do not invent a new section if a suitable one exists.

Then run a self-verification pass: read OFFER.md back and confirm each of the three ROADMAP Phase 14 success criteria is satisfiable from the file alone:
1. Each rung = named package + price/model + one-line outcome.
2. Productised vs bespoke split is visible per rung.
3. The file is structured so Process/Services/About/Intro can each reference a defined offer (named rungs + downstream-consumer notes present).
If any criterion is not satisfiable, fix OFFER.md (this task may edit OFFER.md to close a gap), then re-verify.
  </action>
  <acceptance_criteria>
    - STATE.md contains a reference to `.planning/OFFER.md` describing it as the locked offer spine / content contract for downstream phases. Verify: `grep -c "OFFER.md" .planning/STATE.md` is at least 1.
    - Self-verification confirms all three ROADMAP Phase 14 success criteria are satisfiable from OFFER.md: each rung has name+price+outcome (criterion 1); each rung's productised/bespoke type is readable (criterion 2); named rungs + Downstream Consumer Notes present so phases 15/16/17/19 can reference the offer (criterion 3).
    - No production code files (index.html, css/*, js/*) were modified by this phase. Verify: `git status --porcelain` shows changes only under .planning/.
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -c "OFFER.md" .planning/STATE.md)" -ge 1 ] && [ -z "$(git status --porcelain -- index.html css js 2>/dev/null)" ] && echo PASS</automated>
  </verify>
  <done>STATE.md points to .planning/OFFER.md as the offer spine. The three ROADMAP success criteria are confirmed satisfiable from OFFER.md. No production code touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This phase produces a single static planning Markdown document (`.planning/OFFER.md`) plus a pointer line in `.planning/STATE.md`. There is no runtime, no input handling, no network surface, no executable code, and no data flow. Nothing reaches a browser or a server.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | n/a | .planning/OFFER.md (static markdown) | accept | No attack surface — planning artifact only, no code, no input handling, no runtime. Nothing to mitigate. |

No package installs, no external services, no CLIs. The Package Legitimacy Gate does not apply.
</threat_model>

<verification>
- `.planning/OFFER.md` exists with H1, overview, free-call entry, three rung sections (Diagnose/Strategy/Embed), not-cumulative note, downstream-consumer notes, open items, confirmation note.
- All three ROADMAP Phase 14 success criteria are satisfiable from OFFER.md alone.
- Every provisional price/name/outcome carries `[CONFIRM-KRIS]` (>= 9 markers).
- Productised / Semi-productised / Bespoke split readable per rung.
- Every CTA routes to the free call / Formspree #contact form; no direct buy/booking language.
- No em-dashes in the file body.
- `.planning/STATE.md` records the stable OFFER.md path.
- No production code (index.html, css/*, js/*) modified — `git status --porcelain` confined to `.planning/`.
</verification>

<success_criteria>
- ROADMAP success criterion #1 met: each rung is a named package with a price (or price model) and a one-line outcome.
- ROADMAP success criterion #2 met: the productised vs bespoke split is visible per rung.
- ROADMAP success criterion #3 met: the file is structured so Services (15), Process (16), About (17), Intro (19) can each reference a defined offer.
- All locked decisions D-01..D-10 are carried into the spec; no deferred idea (final numbers, final names, voice pass, direct booking) is treated as decided.
- RESEARCH's three Open Questions are carried forward as flagged open items.
</success_criteria>

<output>
Create `.planning/phases/14-define-the-offer/14-01-SUMMARY.md` when done.
</output>
