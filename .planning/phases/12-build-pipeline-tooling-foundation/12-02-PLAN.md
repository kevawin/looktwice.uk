---
phase: 12-build-pipeline-tooling-foundation
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - package.json
  - bs-config.js
  - playwright.config.js
autonomous: true
requirements: [BUILD-05, BUILD-06]
must_haves:
  truths:
    - "npm test builds first, then runs Playwright against dist/ on port 7777"
    - "Existing contact-form and nav-floating-bar specs pass against built dist/ output"
    - "npm run dev builds, serves dist/ on 3000, and rebuilds on source change with hot reload"
    - "Dev (3000) and test (7777) stay on separate ports, no cross-contamination"
  artifacts:
    - path: "playwright.config.js"
      provides: "webServer that builds then serves dist/ on 7777"
      contains: "npm run build"
    - path: "bs-config.js"
      provides: "browser-sync serving dist/ with source-watch rebuild"
      contains: "dist"
    - path: "package.json"
      provides: "build:watch + build-first test scripts"
      contains: "build:watch"
  key_links:
    - from: "playwright.config.js"
      to: "dist/"
      via: "webServer command builds then http.server --directory dist"
      pattern: "--directory dist"
    - from: "bs-config.js"
      to: "dist/"
      via: "server: 'dist'"
      pattern: "dist"
---

<objective>
Wire dev and test to serve the built `dist/` output (D-04). Playwright builds first, then serves `dist/` on 7777; browser-sync serves `dist/` on 3000, watches source, rebuilds, and hot-reloads. Confirm the existing Playwright suite passes against built output. Keep the dev-3000 / test-7777 split (hard rule, CLAUDE.md).

Purpose: Build-generated features (Phase 11 cutouts) must render live in dev and be tested in CI. This plan makes both tooling surfaces read `dist/`.
Output: Updated `package.json`, `bs-config.js`, `playwright.config.js`; full suite green against dist/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-build-pipeline-tooling-foundation/12-CONTEXT.md
@.planning/phases/12-build-pipeline-tooling-foundation/12-RESEARCH.md
@.planning/phases/12-build-pipeline-tooling-foundation/12-01-SUMMARY.md

<interfaces>
<!-- Existing dev/test config to repoint. Preserve every non-path option. -->

bs-config.js: server '.', port 3000, host lanIp(), open:false, notify:false, ui:false,
  files watch list, callbacks.ready() prints the phone LAN URL. KEEP the lanIp() helper
  and the ready() phone-URL print verbatim — only change what is served + add rebuild.

playwright.config.js: webServer.command = 'python3 -m http.server 7777', port 7777,
  reuseExistingServer:true, timeout 15_000. python3 supports --directory dist (3.7+).

package.json scripts (current): dev (browser-sync --config bs-config.js), test (playwright test),
  test:ui. build (added in 12-01) = node build.js.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint Playwright to build-first, serve dist/ on 7777; run existing suite</name>
  <read_first>
    - playwright.config.js (the file being modified)
    - tests/contact-form.spec.js (asserts no hello@looktwice.uk; uses __LT_FORCE_SUBMIT; goto('/'))
    - tests/nav-floating-bar.spec.js (keys off nav/floating-bar selectors, not hero image)
    - .planning/phases/12-build-pipeline-tooling-foundation/12-RESEARCH.md §"playwright.config.js", Pitfall 5, A3
  </read_first>
  <action>
    Edit playwright.config.js webServer: set command to `npm run build && python3 -m http.server 7777 --directory dist` so the suite always runs against fresh built output served from dist/ (BUILD-06). Raise webServer.timeout from 15_000 to 60_000 (first build includes image encode, can take a few seconds). Keep port 7777, reuseExistingServer:true, baseURL http://localhost:7777, and the three viewport projects unchanged. Do NOT point Playwright at the browser-sync 3000 server (CLAUDE.md hard rule — its injected reload client destabilises the suite).
    Then run the full existing suite against dist/ to confirm contact-form.spec.js and nav-floating-bar.spec.js still pass (Pitfall 5 / A3 — the <img> rewrite must not break nav selectors or reintroduce a mailto).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - playwright.config.js webServer.command contains `npm run build` and `--directory dist`; timeout is 60_000.
    - `npm test` exits 0 — build runs, then all three viewport projects pass.
    - contact-form.spec.js and nav-floating-bar.spec.js (existing specs) pass against dist/ output.
    - build-smoke.spec.js (from 12-01) also passes in the same run.
  </acceptance_criteria>
  <done>Playwright builds dist/ then serves it on 7777; the full existing suite plus build-smoke is green against built output.</done>
</task>

<task type="auto">
  <name>Task 2: browser-sync serves dist/ with source-watch rebuild + hot reload</name>
  <read_first>
    - bs-config.js (the file being modified — preserve lanIp() + ready() phone-URL print)
    - package.json (add build:watch; keep dev invoking the LAN-IP-aware config)
    - build.js (gains a --watch flag in this task)
    - .planning/phases/12-build-pipeline-tooling-foundation/12-RESEARCH.md §"bs-config.js", §"package.json scripts" (option 2 watch pattern, the recommended one)
  </read_first>
  <action>
    Implement the research's option-2 watch pattern (decouples rebuild from reload, no extra dep): add a `--watch` flag to build.js that, after an initial full build, uses `fs.watch` on the source dirs (index.html, css/, js/, images/, _headers) to re-run the build into dist/ on change (debounced). Keep build logic in build.js (one place).
    Repoint bs-config.js: change `server: '.'` to `server: 'dist'`; change the `files` watch list to watch `dist/**` (so browser-sync reloads after the rebuild lands in dist/). Preserve port 3000, host lanIp(), open/notify/ui false, and the callbacks.ready() phone-URL print verbatim.
    Update package.json scripts: add `"build:watch": "node build.js --watch & browser-sync start --config bs-config.js"` so the watcher rebuilds into dist/ and browser-sync (watching dist/**) reloads. Keep `dev` invoking the LAN-IP-aware bs-config.js — set `"dev": "npm run build:watch"`. Keep dev on 3000, tests on 7777 (no cross-contamination).
  </action>
  <verify>
    <automated>node -e "require('./bs-config.js')" && grep -q "server: 'dist'" bs-config.js && grep -q 'build:watch' package.json && node build.js && test -f dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - bs-config.js loads without error, serves `dist`, watches `dist/**`, port 3000, and still prints the phone LAN URL via ready().
    - package.json has `build:watch` and `dev` → `npm run build:watch`.
    - build.js accepts `--watch` and does an initial build before watching (manual confirm: `node build.js --watch` builds dist/index.html, then rebuilds on a touched source file, browser reloads on localhost:3000).
    - Dev port (3000) and test port (7777) remain distinct.
  </acceptance_criteria>
  <done>npm run dev builds dist/, serves it on 3000 with the phone URL printed, and rebuilds + hot-reloads on source change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev/test tooling → dist/ | Dev-only servers read built output; never shipped. No new runtime surface. |

## STRIDE Threat Register

No new runtime attack surface (dev/test tooling only, both gitignored and unshipped). The only carry-over control is keeping the dev-3000 / test-7777 split intact.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | test suite running against stale/source output instead of dist/ | mitigate | playwright webServer builds first + serves --directory dist; existing specs re-run against dist/ confirm parity |
| T-12-04 | (accept) | browser-sync reload client in test runs | accept | dev (3000) and test (7777) kept on separate ports per CLAUDE.md hard rule; Playwright never points at 3000 |
</threat_model>

<verification>
- `npm test` builds then passes the full suite against dist/.
- `npm run dev` serves dist/ on 3000, rebuilds + reloads on source edit, prints phone URL.
- Ports 3000 (dev) and 7777 (test) stay separate.
</verification>

<success_criteria>
- Playwright builds first and serves dist/ on 7777; existing suite green against built output.
- browser-sync serves dist/ on 3000 with source-watch rebuild and hot reload.
- Phone LAN URL print preserved; dev/test port split preserved.
</success_criteria>

<output>
Create `.planning/phases/12-build-pipeline-tooling-foundation/12-02-SUMMARY.md` when done.
</output>
