---
phase: 12-build-pipeline-tooling-foundation
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - build.sh
  - .planning/phases/05-hardening-launch/05-CUTOVER-PLAYBOOK.md
autonomous: false
requirements: [BUILD-07]
must_haves:
  truths:
    - "build.sh runs npm ci + npm run build only on the new-site branch"
    - "build.sh exits 0 without producing dist/ on any other branch (including main)"
    - "Cloudflare Pages build command = bash build.sh, output dir = dist/"
    - "A trivial commit to main leaves the live looktwice.uk holding page unchanged"
    - "The new-site preview deploy serves built dist/ with security headers present"
  artifacts:
    - path: "build.sh"
      provides: "Cloudflare branch-gated build wrapper keyed on CF_PAGES_BRANCH"
      contains: "CF_PAGES_BRANCH"
  key_links:
    - from: "build.sh"
      to: "npm run build"
      via: "gated on CF_PAGES_BRANCH = new-site"
      pattern: "CF_PAGES_BRANCH"
---

<objective>
Add the Cloudflare per-branch build gate (BUILD-07) — the highest-risk item in this phase (research A1, Pitfall 2). Cloudflare's build command is global across branches, so a `build.sh` wrapper gated on `CF_PAGES_BRANCH` builds only on `new-site` and no-ops on `main`. A human-verify checkpoint pushes a trivial commit to `main` and confirms the live holding page is unchanged before the gate is trusted. This task touches the live domain — autonomous: false.

Purpose: Let `new-site` ship the built artifact while `main` (live holding page) keeps deploying as plain static, untouched.
Output: `build.sh`, updated cutover playbook, verified Cloudflare config, confirmed main untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-build-pipeline-tooling-foundation/12-CONTEXT.md
@.planning/phases/12-build-pipeline-tooling-foundation/12-RESEARCH.md
@.planning/phases/12-build-pipeline-tooling-foundation/12-01-SUMMARY.md
@.planning/phases/05-hardening-launch/05-CUTOVER-PLAYBOOK.md

<interfaces>
<!-- Cloudflare injects CF_PAGES_BRANCH at build time (read-only, not a secret). -->
<!-- Dashboard: Build command (global field) + Output directory (global field). -->
<!-- A1 (RESEARCH): Cloudflare does NOT document how main deploys when build.sh no-ops
     under a single project with output dir = dist/. The human checkpoint resolves this. -->
<!-- build.sh shape (RESEARCH §build.sh): set -euo pipefail; if CF_PAGES_BRANCH = new-site
     → npm ci && npm run build; else echo no-build and leave output as the branch's own files. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author build.sh branch gate and update cutover playbook</name>
  <read_first>
    - .planning/phases/12-build-pipeline-tooling-foundation/12-RESEARCH.md §"build.sh — Cloudflare branch gate", Pitfall 2, Open Question 1, Assumptions A1
    - .planning/phases/05-hardening-launch/05-CUTOVER-PLAYBOOK.md (the deploy/cutover doc to amend)
    - package.json (the build script build.sh invokes)
  </read_first>
  <action>
    Create build.sh at repo root: `#!/usr/bin/env bash` + `set -euo pipefail`. If `"$CF_PAGES_BRANCH" = "new-site"`, run `npm ci` then `npm run build` (produces dist/). Else, echo `Branch '$CF_PAGES_BRANCH' — no build (holding page deploys as-is).` and exit 0 WITHOUT creating or altering dist/ (main must ship its own files unchanged). Use `npm ci` (not `npm install`) so the build runs against the committed package-lock.json — supply-chain pin (T-12-SC). Make build.sh executable (`chmod +x build.sh`).
    Update 05-CUTOVER-PLAYBOOK.md: add a section documenting the new build model — Cloudflare Build command = `bash build.sh`, Output directory = `dist/`, gate keyed on CF_PAGES_BRANCH; note A1 (main no-op behaviour is human-verified, not Cloudflare-documented) and the fallback option of two separate Pages projects if the single-project gate proves unreliable. Record that cutover (merge new-site → main) will make main the built branch later — at that point the gate condition must be revisited.
  </action>
  <verify>
    <automated>test -x build.sh && bash -n build.sh && CF_PAGES_BRANCH=main bash build.sh && test ! -d dist/fresh-marker && CF_PAGES_BRANCH=somebranch bash build.sh | grep -q 'no build'</automated>
  </verify>
  <acceptance_criteria>
    - build.sh is executable and passes `bash -n build.sh` (syntax valid).
    - `CF_PAGES_BRANCH=main bash build.sh` exits 0 and prints the no-build message; does not run npm run build.
    - `CF_PAGES_BRANCH=somebranch bash build.sh` output contains "no build".
    - build.sh uses `npm ci` (not `npm install`) in the new-site branch.
    - 05-CUTOVER-PLAYBOOK.md documents the build command, output dir, CF_PAGES_BRANCH gate, A1 caveat, and the two-projects fallback.
  </acceptance_criteria>
  <done>build.sh gates the build on new-site and no-ops on every other branch; cutover playbook records the model and the A1 risk.</done>
</task>

<task type="auto">
  <name>Task 2: Set Cloudflare Pages build config + push new-site, confirm preview serves built dist/</name>
  <read_first>
    - .planning/phases/12-build-pipeline-tooling-foundation/12-RESEARCH.md §"Security Domain" (curl -I header check), Pitfall 1
    - CLAUDE.md §"Phone preview handoff (Cloudflare)" (preview URL model)
    - .planning/STATE.md (current preview URL)
  </read_first>
  <action>
    Set the Cloudflare Pages project build config: Build command = `bash build.sh`, Output directory = `dist/`. Prefer the API/wrangler if credentials are available; if the dashboard is the only route, this becomes an auth gate / human-action — surface the exact dashboard path (Workers and Pages → looktwice.uk → Settings → Build configuration). Commit build.sh + the cutover-playbook update + the 12-01/12-02 changes to the new-site branch and push, triggering a Cloudflare build on new-site.
    After the deploy succeeds, confirm the new-site preview serves the BUILT output: run `curl -sI <branch-preview-url>` and assert the security headers from _headers are present (Strict-Transport-Security, Content-Security-Policy, Permissions-Policy) — proves dist/_headers landed (Pitfall 1 / T-12-01). Confirm the page renders styled (CSS resolved) and the hero image has AVIF/WebP sources.
  </action>
  <verify>
    <automated>git ls-files build.sh | grep -q build.sh</automated>
    <human-check>Cloudflare new-site deploy log shows `bash build.sh` running `npm run build`; the branch preview URL serves a styled page; `curl -sI <preview-url>` returns Strict-Transport-Security + Content-Security-Policy + Permissions-Policy headers.</human-check>
  </verify>
  <acceptance_criteria>
    - Cloudflare Build command = `bash build.sh`, Output directory = `dist/`.
    - new-site preview deploy runs the build and serves dist/.
    - `curl -sI` on the preview shows HSTS, CSP, and Permissions-Policy headers (security headers preserved through the build).
    - Preview page renders styled with the hero <picture> AVIF/WebP sources.
  </acceptance_criteria>
  <done>Cloudflare builds new-site via build.sh into dist/; the preview serves built, styled output with security headers intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm a main commit leaves the live holding page untouched</name>
  <action>Push a trivial safe commit to main, watch the Cloudflare main-branch deploy log to confirm build.sh no-ops (no npm run build), then confirm the live looktwice.uk holding page is visually unchanged. If main is altered, roll back and switch to the two-projects fallback.</action>
  <what-built>
    build.sh gates the Cloudflare build on CF_PAGES_BRANCH so only new-site builds. main (the live looktwice.uk holding page) must keep deploying as plain static, unchanged. Research A1 flags that Cloudflare's exact behaviour when build.sh no-ops under a single project (output dir = dist/) is NOT documented — so it must be verified live before trust.
  </what-built>
  <how-to-verify>
    1. Note the current live looktwice.uk holding page (screenshot or describe what it shows now).
    2. Push a trivial, safe commit to `main` (e.g. a whitespace/comment change in a non-shipped file, or a no-op README touch) — something that triggers a Cloudflare deploy on main but cannot change the visible page.
    3. Watch the Cloudflare deploy log for the main branch: confirm `build.sh` runs and prints the no-build message (does NOT run `npm run build`).
    4. After the main deploy completes, reload https://looktwice.uk and confirm the holding page is visually identical to step 1 — no 404, no empty page, no style/content change.
    5. If main is altered or breaks: STOP. Roll back the main commit. Switch to the two-separate-Pages-projects fallback documented in the cutover playbook (one project watches main with no build, one watches new-site with bash build.sh + dist/).
  </how-to-verify>
  <resume-signal>Type "approved" once looktwice.uk holding page is confirmed unchanged after the main deploy, or describe what changed (triggers the two-projects fallback).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloudflare build config → live domain | The one out-of-repo change; can break the live looktwice.uk holding page if main builds wrongly. |
| package-lock.json → npm ci in CI | Build installs pinned deps in Cloudflare's build environment. |

## STRIDE Threat Register

The only real sensitivity this phase introduces is the Cloudflare config touching the live domain. No new runtime attack surface on the shipped site.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering / DoS (live holding page) | Cloudflare global build command applied to main | mitigate | build.sh gates on CF_PAGES_BRANCH; blocking human-verify (Task 3) confirms main holding page unchanged before trust; two-projects fallback documented (A1) |
| T-12-01 | Info disclosure | security headers dropped on preview | mitigate | Task 2 `curl -sI` asserts HSTS/CSP/Permissions-Policy present on the new-site preview |
| T-12-SC | Tampering | supply-chain in CI build | mitigate | build.sh uses `npm ci` against committed package-lock.json; only sharp + lightningcss added (RESEARCH §Package Legitimacy Audit, Approved) |
</threat_model>

<verification>
- build.sh no-ops on main/other branches, builds on new-site (`bash -n`, env-var runs).
- Cloudflare new-site preview serves built dist/ with security headers (`curl -sI`).
- BLOCKING: a main commit leaves the live holding page unchanged (human-verify).
</verification>

<success_criteria>
- build.sh gates the build on CF_PAGES_BRANCH = new-site; no-ops elsewhere.
- Cloudflare Build command = bash build.sh, output dir = dist/.
- new-site preview serves built, styled output with HSTS/CSP/Permissions-Policy.
- Live looktwice.uk holding page confirmed unchanged after a main deploy (human-verified).
- Cutover playbook records the model, A1 caveat, and two-projects fallback.
</success_criteria>

<output>
Create `.planning/phases/12-build-pipeline-tooling-foundation/12-03-SUMMARY.md` when done.
</output>
