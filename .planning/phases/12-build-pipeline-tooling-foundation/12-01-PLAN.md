---
phase: 12-build-pipeline-tooling-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - build.js
  - tests/build-smoke.spec.js
autonomous: true
requirements: [BUILD-01, BUILD-02, BUILD-03, BUILD-04, BUILD-08]
must_haves:
  truths:
    - "npm run build exits 0 and produces dist/index.html"
    - "dist/css holds 5 minified files with the same names as source"
    - "dist/images holds AVIF + WebP for scene-cafe at widths 480/960/1440/1920"
    - "dist/_headers is byte-identical to source _headers"
    - "dist/js/main.js is byte-identical to source js/main.js (no minify, D-01)"
    - "A second consecutive build skips unchanged image encodes via mtime cache"
  artifacts:
    - path: "build.js"
      provides: "Sequential pure-Node build orchestrator (CSS, images, HTML rewrite, static copy)"
      min_lines: 80
    - path: "tests/build-smoke.spec.js"
      provides: "Build-integrity assertions against dist/"
      contains: "dist/_headers"
    - path: "package.json"
      provides: "build script + sharp/lightningcss devDependencies"
      contains: "\"build\": \"node build.js\""
  key_links:
    - from: "build.js"
      to: "dist/index.html"
      via: "<img> rewrite injecting <source srcset>"
      pattern: "srcset"
    - from: "build.js"
      to: "dist/_headers"
      via: "fs.cpSync static copy"
      pattern: "_headers"
---

<objective>
Stand up the core `build.js` orchestrator and `npm run build`. The build transforms source (committed, authored as-is per D-02) into `dist/` (gitignored): Lightning CSS minifies each of the 5 CSS files, sharp emits AVIF+WebP at 4 widths with an mtime skip-cache, the hero `<img>` is rewritten with responsive `<source srcset>`, and all static assets (`_headers`, `robots.txt`, `favicon.svg`, `fonts/`, `js/`, SVG logos) are copied verbatim into `dist/`.

Purpose: Convert the project from "static files served as-is" to a built artifact. This is the seam Phase 11 extends with cutout codegen.
Output: `build.js`, `tests/build-smoke.spec.js`, updated `package.json` + `package-lock.json`, and a populated `dist/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-build-pipeline-tooling-foundation/12-CONTEXT.md
@.planning/phases/12-build-pipeline-tooling-foundation/12-RESEARCH.md

<interfaces>
<!-- Source layout the build mirrors into dist/ (root-relative paths, Pitfall 4). -->
<!-- CSS link chain in index.html (order matters, preserve names + ?v=17): -->
  /css/tokens.css?v=17 → base.css → layout.css → components.css → animations.css

<!-- Raster image to optimize (the only <img> in index.html): -->
  images/scene-cafe.webp  (hero, eager, fetchpriority="high", width 1600 height 2400)
<!-- kris-portrait.webp is referenced ONLY as an absolute URL in JSON-LD (index.html:42),
     not as an <img>; do NOT rewrite it — copy images/ rasters through unchanged alongside outputs. -->

<!-- Hero markup to rewrite (index.html:112-122): a <picture class="hero__cutout"> wrapping
     a single <img src="/images/scene-cafe.webp" ...>. Rewrite injects two <source> elements
     (AVIF then WebP) before the <img>; keep the <img> as fallback with its alt/width/height/
     loading/decoding/fetchpriority attributes intact. -->

<!-- Static files to copy verbatim into dist/ (Pitfall 1): -->
  _headers, robots.txt, favicon.svg, fonts/ (incl OFL.txt), js/main.js, images/*.svg logos
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps and scaffold build.js with CSS + static-copy steps</name>
  <read_first>
    - package.json (extend devDependencies + scripts, do not replace)
    - .gitignore (confirm dist/ and .cache/ already ignored — they are)
    - _headers, robots.txt, favicon.svg (static files to copy)
    - .planning/phases/12-build-pipeline-tooling-foundation/12-RESEARCH.md §"Lightning CSS", §"Sequential pure-Node build", Pitfall 1, Pitfall 3, Pitfall 4
  </read_first>
  <action>
    Run `npm install --save-dev sharp@0.34.5 lightningcss@1.32.0` (both build tooling, devDependencies per D-01); this also writes package-lock.json. Add `"build": "node build.js"` to package.json scripts.
    Create build.js as a pure-Node async orchestrator (CommonJS `require('node:fs')`, `require('node:path')`). Structure it with named step functions so Phase 11's buildCutout slots in trivially: `build()` calls `cleanDist()` → `buildCss()` → `buildImages()` (Task 2) → `buildHtml(manifest)` (Task 2) → `copyStatic()`. Add a header comment marking the line where the Phase 11 cutout codegen step plugs in (between buildImages and buildHtml).
    `cleanDist()`: `fs.rmSync('dist', {recursive:true, force:true})` then recreate dist/.
    `buildCss()`: use lightningcss `transform()` on EACH of the 5 files in css/ separately (minify:true, targets from `browserslistToTargets(browserslist('>= 0.5%, last 2 versions, not dead'))`); write each to dist/css/<same-name>.css preserving filenames. Do NOT bundle/concatenate (Pitfall 3). If browserslist is not resolvable, fall back to a hardcoded targets object.
    `copyStatic()`: use `fs.cpSync(src, dest, {recursive:true})` to copy into dist/: `_headers`, `robots.txt`, `favicon.svg`, `fonts/` (incl OFL.txt), `js/` (main.js verbatim, NO minify per D-01), and the SVG logos in images/ (black_logo.svg, colour_logo.svg, logo-looktwice.svg, warm_logo.svg, white_logo.svg). Mirror the source directory layout exactly so root-relative paths resolve (Pitfall 4).
    Top-level `build().catch(e => { console.error(e); process.exit(1); })`.
  </action>
  <verify>
    <automated>npm run build && test -f dist/index.html && test -f dist/_headers && diff _headers dist/_headers && diff js/main.js dist/js/main.js && ls dist/css/tokens.css dist/css/base.css dist/css/layout.css dist/css/components.css dist/css/animations.css</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `dist/_headers` exists and `diff _headers dist/_headers` is empty (byte-identical, Pitfall 1).
    - `dist/js/main.js` exists and `diff js/main.js dist/js/main.js` is empty (verbatim, D-01).
    - All 5 css files present in dist/css/ with source filenames; each is smaller than its source (minified).
    - dist/fonts/epilogue-400.woff2, dist/fonts/OFL.txt, dist/favicon.svg, dist/robots.txt all exist.
  </acceptance_criteria>
  <done>build.js installs deps, minifies CSS per-file, copies all static assets byte-identical into dist/, and exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: sharp image pipeline (AVIF+WebP × 4 widths) + HTML srcset rewrite + mtime cache</name>
  <read_first>
    - index.html (lines 112-122 hero <picture>/<img>; lines 28-32 CSS links; the build copies index.html through with the <img> rewritten)
    - build.js (the file from Task 1 — add buildImages + buildHtml steps)
    - .planning/phases/12-build-pipeline-tooling-foundation/12-RESEARCH.md §"sharp — AVIF + WebP", §"<picture> markup", Pattern 2 (mtime cache), Pitfall 5
  </read_first>
  <action>
    Add `buildImages()` to build.js: for each raster source in images/ (currently scene-cafe.webp; kris-portrait.webp is JSON-LD-only so it does NOT need outputs but copying it through is harmless), use sharp to emit AVIF + WebP at widths 480/960/1440/1920. Use `sharp(src).resize({width:w, withoutEnlargement:true})` then `.avif({quality:50, effort:4})` and `.webp({quality:78, effort:4})` (quality numbers are D-03 discretion — tune by eye). Write to dist/images/<baseName>-<w>.avif and -<w>.webp. Return a manifest object keyed by source basename → array of `{w, avif:'/images/<base>-<w>.avif', webp:'/images/<base>-<w>.webp'}`. Phase 11's buildCutout will consume this same manifest, so make it the single source of truth for srcset URLs.
    Add an mtime skip-cache (Pattern 2): read/write `.cache/images.json` (.cache/ already gitignored); before encoding a source, skip if its mtimeMs matches the cached value AND all expected outputs exist; update the cache after encoding. This keeps the dev rebuild loop fast (D-04/BUILD-08).
    Add `buildHtml(manifest)`: read source index.html, rewrite the hero `<picture class="hero__cutout">` block — inject an AVIF `<source>` then a WebP `<source>` (each with the full 4-width srcset from the manifest and `sizes="(max-width: 768px) 85vw, 40vw"`) BEFORE the existing `<img>`. Keep the `<img>` as the fallback with its alt/width/height/loading="eager"/decoding/fetchpriority attributes unchanged. Do NOT touch any other markup (contact form, nav) — Pitfall 5 requires the served HTML still contain no `hello@looktwice.uk`. Write the result to dist/index.html. Keep the CSS `?v=17` links unchanged (discretion: leave static).
  </action>
  <verify>
    <automated>rm -rf .cache dist && npm run build && ls dist/images/scene-cafe-480.avif dist/images/scene-cafe-960.avif dist/images/scene-cafe-1440.avif dist/images/scene-cafe-1920.avif dist/images/scene-cafe-960.webp && grep -q 'type="image/avif"' dist/index.html && grep -q 'srcset' dist/index.html && ! grep -q 'hello@looktwice.uk' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - dist/images/scene-cafe-{480,960,1440,1920}.avif all exist.
    - dist/images/scene-cafe-{480,960,1440,1920}.webp all exist.
    - dist/index.html contains a `<source type="image/avif">` and a `<source type="image/webp">` with 4-width srcset, and still contains the fallback `<img>` with fetchpriority="high".
    - dist/index.html does NOT contain `hello@looktwice.uk` (Pitfall 5).
    - Running build twice in a row: the second run skips re-encoding scene-cafe (mtime cache hit) — observable by the second build completing without re-writing image files (timestamps unchanged) or by a cache-hit log line.
  </acceptance_criteria>
  <done>sharp emits all 8+ image variants, the hero <img> is rewritten with AVIF/WebP srcset, contact markup is untouched, and the mtime cache skips unchanged encodes on rebuild.</done>
</task>

<task type="auto">
  <name>Task 3: build-smoke Playwright spec asserting dist/ integrity</name>
  <read_first>
    - tests/contact-form.spec.js, tests/nav-floating-bar.spec.js (existing spec style; run against dist/ — they are wired in plan 12-02, here just match conventions)
    - build.js (the completed orchestrator)
    - .planning/phases/12-build-pipeline-tooling-foundation/12-RESEARCH.md §"Validation Architecture", Wave 0 Gaps
  </read_first>
  <action>
    Create tests/build-smoke.spec.js — a Playwright spec (matches the existing `@playwright/test` convention; uses node fs, no browser navigation needed) that asserts build-output integrity against dist/. It must check, after a build has run: dist/index.html exists; dist/_headers exists and is byte-identical to source _headers (read both, assert equal); dist/js/main.js byte-identical to source js/main.js; all 5 CSS files present in dist/css/ AND each smaller than its source; the 4-width AVIF and WebP variants for scene-cafe exist in dist/images/; dist/index.html contains `srcset` and `type="image/avif"` and does NOT contain `hello@looktwice.uk`. This spec covers BUILD-01/02/03/04 (the Wave 0 gap from research). Note: this spec reads dist/ directly via fs — it does not need the 7777 web server, so it runs even before plan 12-02 wires the build-first test command.
  </action>
  <verify>
    <automated>npm run build && npx playwright test tests/build-smoke.spec.js --project=desktop-1440</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/build-smoke.spec.js --project=desktop-1440` exits 0 after a build.
    - The spec fails (proves it is real) if dist/_headers is deleted before running — i.e. it genuinely asserts the byte-identical copy.
    - Spec asserts: index.html present, _headers + main.js byte-identical, 5 CSS files smaller than source, 4-width AVIF+WebP present, srcset injected, no mailto leak.
  </acceptance_criteria>
  <done>tests/build-smoke.spec.js green against a fresh dist/ and would catch a dropped _headers, missing image variant, or un-minified CSS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source → dist/ | Build reads committed source, writes generated output. No external/untrusted input. |
| npm registry → node_modules | sharp + lightningcss native binaries pulled at install. |

## STRIDE Threat Register

This is an infra-only phase: no new runtime attack surface, no user input in the pipeline. The security objective is "do not drop the existing controls."

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering / Info disclosure | dist/_headers (HSTS, CSP, Permissions-Policy) | mitigate | build.js copies _headers; build-smoke spec asserts `diff _headers dist/_headers` empty; Pitfall 1 |
| T-12-02 | Tampering | CSP regression from injected markup | mitigate | build only injects `<source srcset>` (no inline script/style); existing `script-src 'self' 'unsafe-inline'` unchanged; spec asserts no mailto reintroduced |
| T-12-SC | Tampering | npm installs (sharp, lightningcss) | mitigate | both top-100 first-party packages, RESEARCH §Package Legitimacy Audit (Approved); pin via package-lock.json; `npm ci` used in CI build (plan 12-03). sharp postinstall is documented libvips prebuild — benign |
</threat_model>

<verification>
- `npm run build` exits 0, dist/ populated.
- `npx playwright test tests/build-smoke.spec.js` green.
- `diff _headers dist/_headers` and `diff js/main.js dist/js/main.js` both empty.
- Second consecutive build skips unchanged image encodes.
</verification>

<success_criteria>
- build.js sequences CSS → images → HTML rewrite → static copy and exits 0.
- dist/ mirrors source layout; all root-relative paths resolve.
- Hero <img> rewritten with AVIF+WebP 4-width srcset; fallback <img> intact.
- _headers and main.js byte-identical in dist/.
- build.js is structured with a clear Phase 11 cutout seam between buildImages and buildHtml.
</success_criteria>

<output>
Create `.planning/phases/12-build-pipeline-tooling-foundation/12-01-SUMMARY.md` when done.
</output>
