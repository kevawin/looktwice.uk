---
quick_id: 260603-x5b
slug: sync-tokens-css-to-phase-13-type-scale
date: 2026-06-03
mode: quick (inline executor — task is fully specified + needs live browser verification)
---

# Quick Task 260603-x5b: Sync css/tokens.css to the Phase 13 type scale

Implements the type-scale follow-up flagged in `13-CONTEXT.md` D-27. The contract
(DESIGN.md) already documents the new scale; this brings `css/tokens.css` into sync.

## Change (css/tokens.css `:root`)

| Token | From | To |
|---|---|---|
| `--text-display` | `clamp(3rem, 7vw, 5.5rem)` | `clamp(2.75rem, 6vw, 5rem)` |
| `--text-headline` | `clamp(1.75rem, 3.5vw, 2.75rem)` | `clamp(2rem, 4vw, 3rem)` |
| `--text-title` | `clamp(1.25rem, 2vw, 1.5rem)` | `clamp(1.5rem, 1.5vw + 1.1rem, 1.875rem)` |
| `--text-lead` | — | `clamp(1.25rem, 1.2vw + 1rem, 1.5rem)` (new) |
| `--text-body` | `clamp(1rem, 1.5vw, 1.1rem)` | `clamp(1.125rem, 1vw + 0.9rem, 1.25rem)` |
| `--text-mega` | `clamp(3.5rem, 9vw, 7rem)` | removed (declared, never used) |
| `--lh-title` | `1.2` | `1.25` |
| `--lh-lead` | — | `1.5` (new) |

## Tasks

1. Edit `css/tokens.css` `:root` per the table. Label/spacing/radii unchanged.
2. Rebuild `dist/` and verify on the live preview: before/after on the homepage,
   confirm body/headings render larger and nothing overflows or breaks. Check
   `--text-mega` removal causes no regression (it was unused).

## Verify
- `grep` confirms no remaining `--text-mega` reference anywhere.
- Live homepage renders the larger body + heading scale; no layout breakage at the
  default preview width.

## Done
- tokens.css holds the new scale; DESIGN.md frontmatter already matches; build clean.
