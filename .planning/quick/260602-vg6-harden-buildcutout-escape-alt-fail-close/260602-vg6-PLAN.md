---
quick_id: 260602-vg6
type: quick
description: Harden buildCutout (escape alt, fail-closed manifest) + fix watch-mode require cache
files_modified:
  - buildCutout.js
  - build.js
  - tests/cutout.spec.js
created: 2026-06-02
---

<objective>
Close the two open follow-ups before the cutout primitive gets reused by other sections:
the code-review warnings WR-01 (unescaped `alt`) and WR-02 (fragile manifest lookup), plus
the watch-mode papercut where `node build.js --watch` served a stale cached `buildCutout.js`.
</objective>

<tasks>

## Task 1: WR-01 + WR-02 (buildCutout.js)
- Escape `alt` via `escapeXml` before it enters the `<title>` (WR-01).
- Resolve the image with an explicit `CUTOUT_IMAGE_WIDTH = 960` and **throw** if no entry
  of that width exists, instead of silently falling back to `entries[1]` (WR-02).
**Verify:** unit tests — escaped `<title>`, fail-closed throw on missing 960 width.

## Task 2: Watch-mode fresh require (build.js)
- Re-require `buildCutout` fresh inside `build()` (`loadBuildCutout` busts the require
  cache) so `--watch` rebuilds pick up edits to `buildCutout.js` without a dev-server restart.
**Verify:** `node build.js` builds the hero correctly via the fresh-require path.

## Task 3: Tests
- Update the existing "missing manifest entry" message assertion; add fail-closed-960 and
  alt-escaping tests in `tests/cutout.spec.js`.
**Verify:** full `npm test` green.

</tasks>
