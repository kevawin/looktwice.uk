---
quick_id: 260602-tm1
type: quick
description: Change hero cutout to two interlocking triangles (demo top band) instead of single rounded-rect
files_modified:
  - buildCutout.js
  - tests/cutout.spec.js
created: 2026-06-02
---

<objective>
Change the hero cutout window from a single rounded-rect to the demo's two interlocking
triangles (down-triangle top-left + up-triangle top-right). The triangle presets carry the
demo's verbatim path geometry and span y 0→519.6, so the hero viewBox/height must drop to
that band so the two triangles fill the window with no empty Hot Pink below.
</objective>

<tasks>

## Task 1: Swap hero shapes + viewBox to the two-triangle band

**Files:** buildCutout.js

**Action:**
In the `hero` entry of `CUTOUT_CONFIGS`:
- Change `shapes` from `[{ type: 'rounded-rect' }]` to
  `[{ type: 'down-triangle' }, { type: 'up-triangle' }]`.
- Change `viewBox` from `'0 0 1000 1064'` to `'0 0 1000 519.6'`.
- Change `height` from `1064` to `519.6`. Keep `width: 1000`.
- Leave `image`, `loading`, `fetchpriority`, and `alt` unchanged.

**Verify:** `node build.js` prints `[buildCutout] injected cutout: hero`; `dist/index.html`
has zero `CUTOUT:hero` markers, exactly one `<image` in the hero SVG, both triangle paths
(`M 83.14,0` and `M 658.43,72.04`) present, and `viewBox="0 0 1000 519.6"`.

**Done:** Hero SVG renders two interlocking triangles revealing the B&W image inside the
solid Hot Pink field; no empty band below.

## Task 2: Update the spec's hero-dimension assertions

**Files:** tests/cutout.spec.js

**Action:**
Update the dist/build-output + browser-render assertions that hardcode the real hero
dimensions (the CLS-guard test asserting `viewBox="0 0 1000 1064"` near line 213-216, plus
any matching intrinsic `height="1064"` assertion) to the new `viewBox="0 0 1000 519.6"` /
`height="519.6"`. Do NOT touch the standalone SHAPE_PRESET unit tests (the down-triangle /
up-triangle d-attribute assertions test presets in isolation and stay as-is). The
single-`<image>` count assertion (D-09) still holds — two triangles share one image.

**Verify:** `npx playwright test tests/cutout.spec.js` is fully green across all projects.

**Done:** The spec locks the new two-triangle hero contract and passes.

</tasks>
