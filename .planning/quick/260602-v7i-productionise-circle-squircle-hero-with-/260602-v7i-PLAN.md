---
quick_id: 260602-v7i
type: quick
description: Productionise circle+squircle hero with focal-point + multi-shape cutout support
files_modified:
  - buildCutout.js
  - build.js
  - index.html
  - tests/cutout.spec.js
  - CLAUDE.md
created: 2026-06-02
---

<objective>
Promote the experimental focal-point work to the real hero. The hero now reveals the
scene-cafe photo through a circle (left) + a squircle (right) instead of two triangles;
the portrait photo is positioned with a per-cutout `focus: {x, y}` (object-position style),
which required the image aspect (`h`) in the build manifest. Lock the new contract behind
tests and document the focus + multi-shape model. The shipped artifact stays static SVG.
</objective>

<tasks>

## Task 1: Implementation (already in working tree — commit as-is)

**Files:** buildCutout.js, build.js, index.html

The implementation is complete and verified in the browser:
- `build.js` — manifest entries carry `h` (intrinsic height) from sharp metadata.
- `buildCutout.js` — `buildSvgString` supports `config.focus = {x, y}` (cover + offset,
  falling back to centred `xMidYMid slice` when absent); hero config uses circle + squircle
  (rx 83 = the triangles' corner radius) with `focus: {x:0.5, y:0.66}`.
- `index.html` — throwaway proof sections removed.

**Verify:** `node build.js` injects the hero; dist hero SVG has one `<image>`, a `<circle>`,
a `<rect rx="83">`, `feColorMatrix`, no base64, marker consumed, preload intact.

## Task 2: Lock the contract + document

**Files:** tests/cutout.spec.js, CLAUDE.md

- Add `h` to the test manifest fixture (matches the real manifest).
- Unit tests: `focus` + known aspect → `preserveAspectRatio="none"` + cover geometry;
  no `focus` → `xMidYMid slice`; multi-shape array → each shape in the mask.
- Build-output: hero contains `<circle`, a squircle `rx="83"`, and a focus-positioned image.
- CLAUDE.md: document `focus: {x, y}` (object-position fractions) and the multi-shape model
  (N shapes share one image/mask; shape coords are in viewBox/image space).

**Verify:** `npx playwright test tests/cutout.spec.js` green; full `npm test` green.

</tasks>
